openapi: 3.0.4
info:
  title: Trinsic API
  version: v1
  x-swaggerexclude: External
servers:
- description: Production server
  url: https://api.trinsic.id
security:
- Bearer: []
paths:
  /api/v1/providers:
    get:
      description: "List all identity providers available to your Organization, including\
        \ their license status."
      operationId: ListProviders
      parameters:
      - description: "Filter by license status. If not specified, returns all providers."
        explode: true
        in: query
        name: licensed
        required: false
        schema:
          type: boolean
        style: form
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/ListProvidersResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/ListProvidersResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/ListProvidersResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: List Providers
      tags:
      - Providers
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
  /api/valpha/redirect-uris:
    get:
      description: List registered Redirect URIs for the authenticated environment
      operationId: List
      parameters:
      - description: Number of pages of uris to return.
        explode: true
        in: query
        name: page
        required: false
        schema:
          default: 1
          format: int32
          maximum: 2147483647
          minimum: 1
          type: integer
        style: form
      - description: Size of the list to be returned. Accepted range from 1 to 100
        explode: true
        in: query
        name: pageSize
        required: false
        schema:
          default: 20
          format: int32
          maximum: 100
          minimum: 1
          type: integer
        style: form
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/ListRedirectUrisResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/ListRedirectUrisResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/ListRedirectUrisResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: List Redirect URIs
      tags:
      - RedirectUris
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
    post:
      description: Add a new redirect uri to the authenticated environment.
      operationId: Add
      requestBody:
        content:
          application/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/AddRedirectUriRequest'
          text/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/AddRedirectUriRequest'
          application/*+json:
            schema:
              allOf:
              - $ref: '#/components/schemas/AddRedirectUriRequest'
        description: "Request for uri to add to the environment. Must be absolute,\
          \ not relative. Wildcard \"*\" accepted."
      responses:
        "201":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/AddRedirectUriResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/AddRedirectUriResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/AddRedirectUriResponse'
          description: Created
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Add Redirect URI
      tags:
      - RedirectUris
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
  /api/valpha/redirect-uris/{id}:
    delete:
      description: Delete a redirect uri from the environment.
      operationId: Delete
      parameters:
      - description: The id of the redirect uri to delete.
        explode: false
        in: path
        name: id
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "204":
          description: No Content
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Delete Redirect URI
      tags:
      - RedirectUris
      x-accepts:
      - application/problem+json
  /api/v1/sessions/{sessionId}/attachments/{attachmentId}/get:
    post:
      description: Fetch an Attachment's contents.
      operationId: GetAttachment
      parameters:
      - description: The ID of the Session to fetch the Attachment from
        explode: false
        in: path
        name: sessionId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: The ID of the Attachment to fetch
        explode: false
        in: path
        name: attachmentId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/GetAttachmentRequest'
          text/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/GetAttachmentRequest'
          application/*+json:
            schema:
              allOf:
              - $ref: '#/components/schemas/GetAttachmentRequest'
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/GetAttachmentResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/GetAttachmentResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/GetAttachmentResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Get Attachment
      tags:
      - Sessions
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
  /api/v1/verification-profiles/{verificationProfileId}/sessions:
    get:
      description: List Sessions created for a specific Verification Profile
      operationId: ListSessions
      parameters:
      - explode: false
        in: path
        name: verificationProfileId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: The field by which sessions should be ordered
        example: Created
        explode: true
        in: query
        name: OrderBy
        required: false
        schema:
          allOf:
          - $ref: '#/components/schemas/SessionOrdering'
          example: Created
        style: form
      - example: Descending
        explode: true
        in: query
        name: OrderDirection
        required: false
        schema:
          allOf:
          - $ref: '#/components/schemas/OrderDirection'
          example: Descending
        style: form
      - description: The number of items to return per page -- must be between `1`
          and `50`
        example: 50
        explode: true
        in: query
        name: PageSize
        required: false
        schema:
          example: 50
          format: int32
          maximum: 50
          minimum: 1
          type: integer
        style: form
      - description: The page number to return -- starts at `1`
        example: 1
        explode: true
        in: query
        name: Page
        required: false
        schema:
          example: 1
          format: int32
          maximum: 2147483647
          minimum: 1
          type: integer
        style: form
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/ListSessionsResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/ListSessionsResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/ListSessionsResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: List Sessions
      tags:
      - Sessions
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
  /api/v1/sessions/{sessionId}:
    get:
      description: Get a Session by its ID
      operationId: GetSession
      parameters:
      - explode: false
        in: path
        name: sessionId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/GetSessionResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/GetSessionResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/GetSessionResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Get Session
      tags:
      - Sessions
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
  /api/v1/sessions/{sessionId}/results:
    post:
      operationId: GetSessionResult
      parameters:
      - explode: false
        in: path
        name: sessionId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/GetSessionResultRequest'
          text/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/GetSessionResultRequest'
          application/*+json:
            schema:
              allOf:
              - $ref: '#/components/schemas/GetSessionResultRequest'
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/GetSessionResultResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/GetSessionResultResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/GetSessionResultResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Get Session Results
      tags:
      - Sessions
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
  /api/v1/sessions/{sessionId}/cancel:
    post:
      description: Cancel a Session by its ID
      operationId: CancelSession
      parameters:
      - explode: false
        in: path
        name: sessionId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/CancelSessionResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/CancelSessionResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/CancelSessionResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Cancel Session
      tags:
      - Sessions
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
  /api/v1/sessions/{sessionId}/redact:
    post:
      description: |-
        Redact a Session, removing all identity data from Trinsic's servers.
        Every verification profile has a redaction period that dictates how long we will hold on to your users' PII data. Once a session falls outside the redaction cutoff date, all PII will automatically be removed from that session.
        You can utilize this endpoint to redact a session immediately.
      operationId: RedactSession
      parameters:
      - explode: false
        in: path
        name: sessionId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Redact Session
      tags:
      - Sessions
      x-accepts:
      - application/problem+json
  /api/v1/sessions/provider/direct:
    post:
      description: "Verify a user's identity with a specific provider, handling additional\
        \ user interaction in your own UI. \n\nSignal which kinds of user interactions\
        \ your UI can handle using the `Capabilities` field. \n\nIf `FallbackToHostedUi`\
        \ is `true`, Trinsic's hosted UI will automatically be invoked to handle any\
        \ capabilities you do not support."
      operationId: CreateDirectProviderSession
      requestBody:
        content:
          application/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/CreateDirectProviderSessionRequest'
              required:
              - verificationProfileId
          text/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/CreateDirectProviderSessionRequest'
              required:
              - verificationProfileId
          application/*+json:
            schema:
              allOf:
              - $ref: '#/components/schemas/CreateDirectProviderSessionRequest'
              required:
              - verificationProfileId
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/CreateDirectProviderSessionResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/CreateDirectProviderSessionResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/CreateDirectProviderSessionResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Create Direct Provider Session
      tags:
      - Sessions
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
  /api/v1/sessions/{sessionId}/step/refresh:
    post:
      description: Refreshes the content of a Step for a Direct Provider Session.
      operationId: RefreshStepContent
      parameters:
      - explode: false
        in: path
        name: sessionId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/RefreshStepContentRequest'
          text/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/RefreshStepContentRequest'
          application/*+json:
            schema:
              allOf:
              - $ref: '#/components/schemas/RefreshStepContentRequest'
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/RefreshStepContentResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/RefreshStepContentResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/RefreshStepContentResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Refresh Step Content
      tags:
      - Sessions
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
  /api/v1/sessions/{sessionId}/native-challenge/submit:
    post:
      description: "Submits the response from a Native Challenge (e.g., mDL exchange\
        \ via DC API) and processes the results."
      operationId: SubmitNativeChallengeResponse
      parameters:
      - explode: false
        in: path
        name: sessionId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/SubmitNativeChallengeResponseRequest'
          text/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/SubmitNativeChallengeResponseRequest'
          application/*+json:
            schema:
              allOf:
              - $ref: '#/components/schemas/SubmitNativeChallengeResponseRequest'
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/SubmitNativeChallengeResponseResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/SubmitNativeChallengeResponseResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/SubmitNativeChallengeResponseResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Submit Native Challenge Response
      tags:
      - Sessions
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
  /api/v1/sessions/provider/hosted:
    post:
      description: "Verify a user's identity with a specific provider, using Trinsic-hosted\
        \ UI for providers which require additional user interaction."
      operationId: CreateHostedProviderSession
      requestBody:
        content:
          application/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/CreateHostedProviderSessionRequest'
              required:
              - verificationProfileId
          text/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/CreateHostedProviderSessionRequest'
              required:
              - verificationProfileId
          application/*+json:
            schema:
              allOf:
              - $ref: '#/components/schemas/CreateHostedProviderSessionRequest'
              required:
              - verificationProfileId
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/CreateHostedProviderSessionResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/CreateHostedProviderSessionResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/CreateHostedProviderSessionResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Create Hosted Provider Session
      tags:
      - Sessions
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
  /api/v1/sessions/providers/recommend:
    post:
      description: |-
        Recommend providers for a specific user session.
        You can filter based on health (default=online) and specify country and subdivision information.
        Trinsic will use the phone number and IP address, if provided, to deduce the country and subdivision of the user and use that info for filtering the providers.
      operationId: RecommendProviders
      requestBody:
        content:
          application/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/RecommendProvidersRequest'
          text/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/RecommendProvidersRequest'
          application/*+json:
            schema:
              allOf:
              - $ref: '#/components/schemas/RecommendProvidersRequest'
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/RecommendProvidersResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/RecommendProvidersResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/RecommendProvidersResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Recommend Providers
      tags:
      - Sessions
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
  /api/v1/sessions/widget:
    post:
      description: Verify a user's identity using Trinsic's hosted Widget flow.
      operationId: CreateWidgetSession
      requestBody:
        content:
          application/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/CreateWidgetSessionRequest'
              required:
              - verificationProfileId
          text/json:
            schema:
              allOf:
              - $ref: '#/components/schemas/CreateWidgetSessionRequest'
              required:
              - verificationProfileId
          application/*+json:
            schema:
              allOf:
              - $ref: '#/components/schemas/CreateWidgetSessionRequest'
              required:
              - verificationProfileId
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/CreateWidgetSessionResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/CreateWidgetSessionResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/CreateWidgetSessionResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Create Widget Session
      tags:
      - Sessions
      x-content-type: application/json
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
  /api/valpha/verification-profiles:
    get:
      description: List the verification profiles for the current organization and
        environment.
      operationId: ListVerificationProfiles
      parameters:
      - description: ""
        explode: true
        in: query
        name: page
        required: false
        schema:
          default: 1
          format: int32
          maximum: 2147483647
          minimum: 1
          type: integer
        style: form
      - description: Size of the list to be returned. Accepted range from 1 to 100
        explode: true
        in: query
        name: pageSize
        required: false
        schema:
          default: 20
          format: int32
          maximum: 100
          minimum: 1
          type: integer
        style: form
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/ListVerificationProfilesResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/ListVerificationProfilesResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/ListVerificationProfilesResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: List Verification Profiles
      tags:
      - VerificationProfiles
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
    post:
      description: Creates a new verification profile within your organization.
      operationId: CreateVerificationProfile
      requestBody:
        content:
          multipart/form-data:
            encoding:
              Alias:
                style: form
              BrandName:
                style: form
              PrimaryColor:
                style: form
              Providers:
                style: form
              Logo:
                style: form
              IsProductionUsage:
                style: form
            schema:
              $ref: '#/components/schemas/CreateVerificationProfile_request'
      responses:
        "201":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/CreateVerificationProfileResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/CreateVerificationProfileResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/CreateVerificationProfileResponse'
          description: Created
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Create Verification Profile
      tags:
      - VerificationProfiles
      x-content-type: multipart/form-data
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
  /api/valpha/verification-profiles/{id}:
    get:
      description: Gets a specific verification profile by ID.
      operationId: GetVerificationProfileById
      parameters:
      - explode: false
        in: path
        name: id
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/VerificationProfileResponse'
            application/json:
              schema:
                $ref: '#/components/schemas/VerificationProfileResponse'
            text/json:
              schema:
                $ref: '#/components/schemas/VerificationProfileResponse'
          description: OK
        "400":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.5.1
                title: Bad Request
                detail: This feature is not supported for your configruation
                status: 400
                traceId: 00-3fcbc632c5caaca825849d495c62d7b0-09a3500f89acbbcc-00
                instance: /api/v1/sample-request-path
                errors:
                  PropertyName:
                  - Specific error description for property
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Bad request
        "401":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Unauthorized
                status: 401
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Forbidden
                status: 403
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Forbidden
        "500":
          content:
            application/problem+json:
              example:
                type: https://tools.ietf.org/html/rfc7231#section-6.6.1
                title: Internal Server Error
                status: 500
                traceId: 00-982607166a542147b435be3a847ddd71-fc75498eb9f09d48-00
              schema:
                $ref: '#/components/schemas/ProblemDetails'
          description: Internal server error
      summary: Get Verification Profile
      tags:
      - VerificationProfiles
      x-accepts:
      - application/json
      - application/problem+json
      - text/json
      - text/plain
components:
  schemas:
    AadhaarInput:
      properties:
        fullName:
          description: The user's full name
          nullable: true
          type: string
        dateOfBirth:
          description: "The user's date of birth, in `YYYY-MM-DD` format"
          format: date
          nullable: true
          type: string
      type: object
    AddRedirectUriRequest:
      example:
        uri: uri
      properties:
        uri:
          minLength: 1
          type: string
      required:
      - uri
      type: object
    AddRedirectUriResponse:
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
      properties:
        id:
          format: uuid
          type: string
      required:
      - id
      type: object
    Address:
      description: Address information for an individual
      properties:
        line1:
          nullable: true
          type: string
        line2:
          nullable: true
          type: string
        line3:
          nullable: true
          type: string
        city:
          nullable: true
          type: string
        subdivision:
          description: "The subdivision code of the address.\n            \nSubdivision\
            \ refers to a state, province, or other similar division of a country.\n\
            Examples include \"CA\" for California, \"QC\" for Québec, \"MD\" for\
            \ Madrid, or \"NSW\" for New South Wales."
          nullable: true
          type: string
        postalCode:
          nullable: true
          type: string
        country:
          nullable: true
          type: string
        fullAddress:
          description: The full address as a single string
          nullable: true
          type: string
      type: object
    AppleWalletInput:
      properties:
        exchangeMechanism:
          allOf:
          - $ref: '#/components/schemas/MdlExchangeMechanism'
          description: "The exchange mechanism to use for this Apple Wallet verification.\n\
            \            \nUse `DigitalCredentialsApi` for Digital Credentials API\
            \ on web, or `NativeApp` for a native iOS app."
          nullable: true
      type: object
    AppleWalletProviderOutput:
      description: Exposed properties for the `apple-wallet` Provider which do not
        directly map to the normalized IdentityData model.
      properties:
        rawMdlOutput:
          allOf:
          - $ref: '#/components/schemas/MdlOutput'
          description: The raw output of the mDL exchange performed through Apple
            Wallet.
      required:
      - rawMdlOutput
      type: object
    AttachmentInfo:
      properties:
        id:
          description: "The unique identifier of the Attachment.\n            \nPass\
            \ this to the `GetAttachment` endpoint, along with the Session ID and\
            \ the `resultsAccessKey` for said Session."
          format: uuid
          type: string
        type:
          description: "The type of the Attachment.\n            \nPossible values:\n\
            - \"selfie\"\n- \"document_front\"\n- \"document_back\"\n- \"document_portrait\"\
            \n- \"document_signature\"\n- \"document_scan_report\"\n            \n\
            Additional attachment types may be defined for specific Providers."
          type: string
        contentType:
          description: "The MIME type of the attachment's contents; eg, \"image/jpeg\"\
            \ or \"application/pdf\"."
          type: string
        sizeBytes:
          description: The size in bytes of the attachment.
          format: int32
          type: integer
      required:
      - contentType
      - id
      - sizeBytes
      - type
      type: object
    AttributeAvailability:
      description: Indicates when an attribute will be available in verification results.
      enum:
      - Never
      - Sometimes
      - Always
      type: string
      x-enumDescriptions:
        Never: This attribute will never be present in verification results.
        Sometimes: This attribute may or may not be present depending on the verification
          context.
        Always: This attribute will always be present in verification results.
    AustriaHandySignaturProviderOutput:
      description: Exposed properties for the `a-at-handy-signatur-login` Provider
        which do not directly map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name of the verified individual
          example: John
          type: string
        lastName:
          description: The last name of the verified individual
          example: Doe
          type: string
      required:
      - firstName
      - lastName
      type: object
    BelgiumIdCardProviderOutput:
      description: Exposed properties for the `a-be-id-login` Provider which do not
        directly map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name of the verified individual
          example: John
          type: string
        lastName:
          description: The last name of the verified individual
          example: Doe
          type: string
        dateOfBirth:
          description: The date of birth of the verified individual
          example: 1985-12-10
          format: date
          type: string
        nationalRegisterNumber:
          description: "The Belgian National Register Number (\"Rijksregisternummer\"\
            ) of the verified individual.\n            \nThis is an 11-digit number\
            \ in the format YYMMDDXXXCC, where:\n- YYMMDD represents the individual's\
            \ date of birth (year, month, day).\n- XXX is a sequential birth number,\
            \ odd for males and even for females.\n- CC is a checksum, calculated\
            \ with the equation: 97 - (YYMMDDXXX mod 97)\n            \nFor births\
            \ in the year 2000 or later, the digit '2' is prepended to the first 9\
            \ digits during checksum calculation."
          example: "83122412413"
          type: string
      required:
      - dateOfBirth
      - firstName
      - lastName
      - nationalRegisterNumber
      type: object
    BrazilCpfCheckInput:
      properties:
        cpfNumber:
          description: "The user's 11-digit, numeric CPF Number"
          maxLength: 11
          minLength: 11
          pattern: ^\d*$
          type: string
      required:
      - cpfNumber
      type: object
    BrazilDigitalCnhInput:
      properties:
        cpfNumber:
          description: "The user's 11-digit, numeric CPF Number"
          maxLength: 11
          minLength: 11
          pattern: ^\d*$
          type: string
        digitalCnhFile:
          description: The raw bytes of the digital CNH file collected from the user.
          format: byte
          nullable: true
          type: string
        digitalCnhFileContentType:
          description: "The MIME Type of the file contained in `DigitalCnhFile`.\n\
            \            \nMust be one of `application/pdf`, `image/jpeg`, or `image/png`."
          nullable: true
          type: string
        facialBiometryPhoto:
          description: "The raw bytes of the image of the user's face, collected for\
            \ biometric comparison."
          format: byte
          nullable: true
          type: string
      required:
      - cpfNumber
      type: object
    CancelSessionResponse:
      example:
        session: ""
      properties:
        session:
          allOf:
          - $ref: '#/components/schemas/Session'
      required:
      - session
      type: object
    ContractAttachment:
      description: Information about an attachment that a Provider may return in verification
        results.
      example:
        type: document_portrait
        outputted: Always
      properties:
        type:
          description: "The type of the attachment as it appears in verification results.\n\
            \            \nPossible values:\n- \"selfie\"\n- \"document_front\"\n\
            - \"document_back\"\n- \"document_portrait\"\n- \"document_signature\"\
            \n- \"document_scan_report\"\n            \nAdditional attachment types\
            \ may be defined for specific Providers."
          example: document_portrait
          type: string
        outputted:
          allOf:
          - $ref: '#/components/schemas/AttributeAvailability'
          description: Indicates when this attachment will be present in verification
            results.
          example: Always
          x-enumDescriptions:
            Never: This attribute will never be present in verification results.
            Sometimes: This attribute may or may not be present depending on the verification
              context.
            Always: This attribute will always be present in verification results.
      required:
      - outputted
      - type
      type: object
    ContractAttribute:
      description: Information about an attribute that a Provider may return in verification
        results.
      example:
        scope: person.givenName
        outputted: Always
      properties:
        scope:
          description: The scope of the attribute as it appears in verification results.
          example: person.givenName
          type: string
        outputted:
          allOf:
          - $ref: '#/components/schemas/AttributeAvailability'
          description: Indicates when this attribute will be present in verification
            results.
          example: Always
          x-enumDescriptions:
            Never: This attribute will never be present in verification results.
            Sometimes: This attribute may or may not be present depending on the verification
              context.
            Always: This attribute will always be present in verification results.
      required:
      - outputted
      - scope
      type: object
    CoteDIvoireNidLookup2Input:
      properties:
        idNumber:
          description: The user's National ID number (NNI) or document number
          nullable: true
          type: string
      type: object
    CreateDirectProviderSessionRequest:
      example:
        fallbackToHostedUI: true
        redirectUrl: redirectUrl
        capabilities:
        - LaunchBrowser
        - LaunchBrowser
        provider: provider
        providerInput: ""
        verificationProfileId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
      properties:
        provider:
          description: The ID of the provider to launch
          minLength: 1
          type: string
        verificationProfileId:
          description: The ID of the Verification Profile to use for this session.
          format: uuid
          type: string
        redirectUrl:
          description: "The Redirect URL to which the user should be sent after the\
            \ session is complete.\n            \nThis field is required for providers\
            \ which employ a redirect-based flow."
          nullable: true
          type: string
        capabilities:
          description: "The list of capabilities your integration supports. Capabilities\
            \ are the core of Trinsic's whitelabel-with-optional-fallback offering.\n\
            \            \nMost capabilities align with either an `IntegrationLaunchMethod`\
            \ or an `IntegrationCollectionMethod`.\nThe exception being refresh content\
            \ to support updating the content of the launch method.\n            \n\
            For example, to support a basic redirect-based flow, you must include\
            \ the `LaunchRedirect` and `CaptureRedirect` capabilities.\nTo support\
            \ a mobile deeplink / polling flow, you must include the `DeeplinkToMobile`\
            \ and `PollForResults` capabilities.\n            \nIf `FallbackToHostedUi`\
            \ is `true`, Trinsic will automatically fall back to a Trinsic-hosted\
            \ UI to cover any gaps in your integration's capabilities.\nIf `FallbackToHostedUi`\
            \ is `false`, gaps in your integration's capabilities will result in an\
            \ error during Session creation.\n            \nRead more on how to integrate\
            \ at <a href=\"https://docs.trinsic.id/docs/direct-provider-sessions\"\
            >the guide on Direct Provider Sessions</a>"
          items:
            $ref: '#/components/schemas/IntegrationCapability'
          minItems: 1
          type: array
        fallbackToHostedUI:
          description: "Whether the session should fall back to a Trinsic-hosted UI\
            \ in certain instances.\n            \nSpecifically, fallback will occur\
            \ if any of the following are true:\n- You attempted to launch a provider\
            \ which requires a capability you did not express support for\n    - In\
            \ this case, Trinsic's hosted UI will perform the necessary capability\n\
            - You attempted to launch a provider which requires input, and the input\
            \ was either not provided or incomplete\n    - In this case, Trinsic's\
            \ hosted UI will collect the necessary input from the user\n         \
            \   \nIf fallback occurs, the session's NextStep will always be LaunchBrowser,\n\
            and the CollectionMethod will always be CaptureRedirect.\n           \
            \ \nIf this field is set to `true`, you must also:\n1. Set the `RedirectUrl`\
            \ field to a non-empty value\n2. Include the `LaunchBrowser` and `CaptureRedirect`\
            \ capabilities in the `Capabilities` field"
          nullable: true
          type: boolean
        providerInput:
          allOf:
          - $ref: '#/components/schemas/ProviderInput'
          description: Provider-specific input for those providers which require it.
          nullable: true
      required:
      - capabilities
      - provider
      - verificationProfileId
      type: object
    CreateDirectProviderSessionResponse:
      example:
        resultCollection: ""
        nextStep: ""
        sessionId: 00000000-0000-0000-0000-000000000000
      properties:
        sessionId:
          description: The ID of the newly-created Acceptance Session
          example: 00000000-0000-0000-0000-000000000000
          format: uuid
          type: string
        resultCollection:
          allOf:
          - $ref: '#/components/schemas/ResultCollection'
          description: The method by which you must collect the results of the Acceptance
            Session.
        nextStep:
          allOf:
          - $ref: '#/components/schemas/IntegrationStep'
          description: The next step you must take to launch the user into the integration
      required:
      - nextStep
      - resultCollection
      - sessionId
      type: object
    CreateHostedProviderSessionRequest:
      example:
        redirectUrl: redirectUrl
        provider: provider
        providerInput: ""
        verificationProfileId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
      properties:
        provider:
          description: The ID of the provider to launch
          minLength: 1
          type: string
        verificationProfileId:
          description: The ID of the Verification Profile to use for this session.
          format: uuid
          type: string
        redirectUrl:
          description: The Redirect URL to which the user should be sent after the
            session is complete.
          minLength: 1
          type: string
        providerInput:
          allOf:
          - $ref: '#/components/schemas/ProviderInput'
          deprecated: true
          description: |-
            Provider-specific input for those providers which require it.


            <b>Deprecated:</b> In the future, Hosted Provider Sessions will not accept input on creation,
            and will instead always redirect the user to a hosted interface to collect input. If you need
            to collect input from the user yourself, please use the Create Direct Session endpoint instead.
          nullable: true
      required:
      - provider
      - redirectUrl
      - verificationProfileId
      type: object
    CreateHostedProviderSessionResponse:
      example:
        sessionId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        launchUrl: https://verify.trinsic.id/api/session/launch?clientToken=...
        resultsAccessKey: 417zNGHMJp6Ht2jCoktTLNGDRZEt4BqTwwQ8Q9ZaQz8aS4Jd7nD1xff2WLhPS69j5q9QT4gvgV79Gv7QtaXwu5YC9Utr35...
      properties:
        sessionId:
          description: The ID of the newly-created Acceptance Session
          format: uuid
          type: string
        launchUrl:
          description: "The URL that should be used to launch the Hosted Provider\
            \ Session on your user's device.\n            \nThere are two recommended\
            \ ways to do so:\n- Redirect their browser to the `LaunchUrl` as a top-level\
            \ navigation\n- Open a popup window and navigate it to the `LaunchUrl`\n\
            \            \nThis URL is sensitive and as such can only be obtained\
            \ once. If you need to obtain it again, you will need to create a new\
            \ Acceptance Session."
          example: https://verify.trinsic.id/api/session/launch?clientToken=...
          type: string
        resultsAccessKey:
          description: "The `resultsAccessKey` for the Acceptance Session.\n     \
            \       \nThis is an encrypted payload which contains the decryption key\
            \ necessary to access the Session's Data Vault.\n            \nSave this\
            \ securely in your systems; it must be passed back with any API call which\
            \ requires access to the Session's Data Vault.\n            \nTrinsic\
            \ cannot access a Session's Data Vault without this key."
          example: 417zNGHMJp6Ht2jCoktTLNGDRZEt4BqTwwQ8Q9ZaQz8aS4Jd7nD1xff2WLhPS69j5q9QT4gvgV79Gv7QtaXwu5YC9Utr35...
          type: string
      required:
      - launchUrl
      - resultsAccessKey
      - sessionId
      type: object
    CreateVerificationProfileResponse:
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
      properties:
        id:
          description: The ID of the verification profile
          format: uuid
          type: string
      required:
      - id
      type: object
    CreateWidgetSessionRequest:
      example:
        redirectUrl: redirectUrl
        verificationProfileId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        recommendationInfo: ""
        providers:
        - providers
        - providers
      properties:
        verificationProfileId:
          description: The ID of the Verification Profile to use for this session.
          format: uuid
          type: string
        redirectUrl:
          description: "The URL to redirect the user to after the widget session is\
            \ complete.\n            \n*Note*: this should NOT be set if you intend\
            \ to use Trinsic's Web UI SDK to launch the Widget\nas a popup; in that\
            \ case, session resolution is handled by our SDK, not via redirect."
          nullable: true
          type: string
        providers:
          description: "The list of allowed identity providers. If not specified,\
            \ all available providers will be allowed."
          items:
            type: string
          nullable: true
          type: array
        recommendationInfo:
          allOf:
          - $ref: '#/components/schemas/RecommendationInfo'
          description: |-
            Data that you already know about the user being verified.


            This data is used to improve the user experience during provider selection, by surfacing the most relevant providers first.
          nullable: true
      required:
      - verificationProfileId
      type: object
    CreateWidgetSessionResponse:
      example:
        sessionId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        launchUrl: https://verify.trinsic.id/api/session/launch?clientToken=...
        resultsAccessKey: 417zNGHMJp6Ht2jCoktTLNGDRZEt4BqTwwQ8Q9ZaQz8aS4Jd7nD1xff2WLhPS69j5q9QT4gvgV79Gv7QtaXwu5YC9Utr35...
      properties:
        sessionId:
          description: The ID of the newly-created Acceptance Session
          format: uuid
          type: string
        launchUrl:
          description: "The URL that should be used to invoke the Acceptance Session\
            \ on your user's device.\n            \nYou can use our frontend SDKs\
            \ to launch the user into the Acceptance Session, or you can redirect\
            \ the user's browser to this URL.\n            \nThis URL is sensitive\
            \ and as such can only be obtained once. If you need to obtain it again,\
            \ you will need to create a new Acceptance Session."
          example: https://verify.trinsic.id/api/session/launch?clientToken=...
          type: string
        resultsAccessKey:
          description: "The `resultsAccessKey` for the Acceptance Session.\n     \
            \       \nThis is an encrypted payload which contains the decryption key\
            \ necessary to access the Session's Data Vault.\n            \nSave this\
            \ securely in your systems; it must be passed back with any API call which\
            \ requires access to the Session's Data Vault.\n            \nTrinsic\
            \ cannot access a Session's Data Vault without this key."
          example: 417zNGHMJp6Ht2jCoktTLNGDRZEt4BqTwwQ8Q9ZaQz8aS4Jd7nD1xff2WLhPS69j5q9QT4gvgV79Gv7QtaXwu5YC9Utr35...
          type: string
      required:
      - launchUrl
      - resultsAccessKey
      - sessionId
      type: object
    CzechiaMojeIdProviderOutput:
      description: Exposed properties for the `a-mojeid` Provider which do not directly
        map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name of the verified individual
          example: John
          type: string
        lastName:
          description: The last name of the verified individual
          example: Doe
          type: string
        dateOfBirth:
          description: The date of birth of the verified individual
          example: 1985-12-10
          format: date
          type: string
        openId2Identifier:
          description: "The OpenID 2.0 Identifier (\"openid2_id\") of the verified\
            \ individual's MojeID account.\n            \nThis is an HTTPS URL which\
            \ uniquely identifies the user within the MojeID system."
          example: https://tpalts3.mojeid.regtest.nic.cz/#jjczaJE2Hfsg
          type: string
      required:
      - dateOfBirth
      - firstName
      - lastName
      - openId2Identifier
      type: object
    DocumentData:
      properties:
        type:
          allOf:
          - $ref: '#/components/schemas/DocumentType'
          description: The type of the document.
          example: DriversLicense
          nullable: true
        number:
          description: The primary identifying number of the document.
          example: D1234567
          nullable: true
          type: string
        issueDate:
          description: The date the document was issued.
          example: 2025-01-15
          format: date
          nullable: true
          type: string
        expirationDate:
          description: The date the document expires.
          example: 2030-01-15
          format: date
          nullable: true
          type: string
        issuingCountry:
          description: The ISO 3166-1 alpha-2 country code of the country that issued
            the document.
          example: US
          nullable: true
          type: string
        issuingSubdivision:
          description: "The ISO 3166-2 subdivision code of the issuing authority which\
            \ issued the document.\n            \nThis is always in the form {CountryCode}-{SubdivisionCode},\
            \ where CountryCode is 2 letters and SubdivisionCode is 1-3 alphanumeric\
            \ characters."
          example: US-NY
          nullable: true
          type: string
        issuingAuthority:
          description: The name of the authority which issued the document.
          nullable: true
          type: string
      type: object
    DocumentType:
      enum:
      - Passport
      - DriversLicense
      - NationalId
      - IdCard
      - Other
      type: string
      x-enumDescriptions:
        Passport: ""
        DriversLicense: ""
        NationalId: ""
        IdCard: ""
        Other: ""
    EstoniaIdCardProviderOutput:
      description: Exposed properties for the `a-ee-web-eid-login` Provider which
        do not directly map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name of the verified individual
          example: John
          type: string
        lastName:
          description: The last name of the verified individual
          example: Doe
          type: string
        dateOfBirth:
          description: The date of birth of the verified individual
          example: 1985-12-10
          format: date
          type: string
        personalIdentificationCode:
          description: "The 11-digit Estonian personal identification code (\"isikukood\"\
            ) of the verified individual.\n            \nThis is in the format GYYMMDDSSSC,\
            \ where:\n- G combines the century and sex of birth\n- YYMMDD is the date\
            \ of birth within the given century\n- SSS is a serial number distinguishing\
            \ people born on the same date\n- C is a checksum digit\n            \n\
            If G is odd, the individual is male.\nIf G is even, the individual is\
            \ female.\n            \nIf G is 1 or 2, the individual was born in the\
            \ 19th century (1800-1899).\nIf G is 3 or 4, the individual was born in\
            \ the 20th century (1900-1999).\nIf G is 5 or 6, the individual was born\
            \ in the 21st century (2000-2099).\nIf G is 7 or 8, the individual was\
            \ born in the 22nd century (2100-2199)."
          example: "39006301234"
          type: string
      required:
      - dateOfBirth
      - firstName
      - lastName
      - personalIdentificationCode
      type: object
    FaydaProviderOutput:
      description: Exposed properties for the `ethiopia-fayda` Provider which do not
        directly map to the normalized IdentityData model.
      properties:
        sub:
          description: "A unique eKYC identifying token used to match the original\
            \ eKYC token received from the provider when the user was initially registered.\n\
            \            \nSince Fayda does not return identifying data, it is the\
            \ responsibility of the relying party to keep the unique user token received\
            \ from Fayda when\nthe user was initially registered to do a comparison\
            \ of the subs to verify that it is the same person."
          example: F50278D6-7C38-4284-83CD-549BBC818F5C
          nullable: true
          type: string
      type: object
    FinlandIdCardProviderOutput:
      description: Exposed properties for the `a-fi-id-login` Provider which do not
        directly map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name of the verified individual
          example: John
          type: string
        lastName:
          description: The last name of the verified individual
          example: Doe
          type: string
        dateOfBirth:
          description: The date of birth of the verified individual
          example: 1985-12-10
          format: date
          type: string
        personalIdentificationCode:
          description: "The 11-digit Finnish Personal Identification Code (Henkilö\
            tunnus) of the verified individual.\n            \n This is in the format\
            \ DDMMYYCZZZQ, where:\n- DDMMYY is the date of birth\n- C is a symbol\
            \ which determines the century of birth\n- ZZZ is an individual number,\
            \ indicating gender\n- Q is a checksum character\n            \nIf ZZZ\
            \ is even, the individual is female.\nIf ZZZ is odd, the individual is\
            \ male.\n            \nIf C is '+', the individual was born in the 19th\
            \ century (1800-1899).\nIf C is '-', 'U', 'V', 'W', 'X', or 'Y', the individual\
            \ was born in the 20th century (1900-1999).\nIf C is 'A', 'B', 'C', 'D',\
            \ 'E', or 'F', the individual was born in the 21st century (2000-2099)."
          example: 300690-543X
          type: string
      required:
      - dateOfBirth
      - firstName
      - lastName
      - personalIdentificationCode
      type: object
    FrejaProviderOutput:
      description: Exposed properties for the `a-freja-eid-login` Provider which do
        not directly map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name of the verified individual
          example: John
          type: string
        lastName:
          description: The last name of the verified individual
          example: Doe
          type: string
        personalNumber:
          description: "The value returned by Freja in the \"ssn\" field.\n      \
            \      \nThe actual value of this field depends on the country of origin\
            \ used to create the Freja credential. It is typically\na Social Security\
            \ Number, National Identification Number, or equivalent personal identifier."
          example: "200003276624"
          type: string
        personalNumberCountry:
          description: The 2-digit ISO country code of the country which issued the
            personal number.
          example: SE
          type: string
      required:
      - firstName
      - lastName
      - personalNumber
      - personalNumberCountry
      type: object
    FrenchNumeriqueGivenName:
      description: Given name information including the full given name and its structured
        components.
      properties:
        fullGivenName:
          description: |-
            Full given name(s) as recorded on the user's French identity document.
            If the user has multiple given names, they are separated by a single space.
          example: Jean Pierre
          nullable: true
          type: string
        firstName:
          description: First/primary given name (premier prénom).
          example: Jean
          nullable: true
          type: string
        middleName:
          description: "Middle name(s) (prénoms secondaires), separated by spaces\
            \ if multiple."
          example: Pierre
          nullable: true
          type: string
      type: object
    FrenchNumeriqueNationality:
      description: Nationality information with ISO 3166 alpha-3 code and French label.
      properties:
        code:
          description: ISO 3166 alpha-3 country code.
          example: FRA
          nullable: true
          type: string
        label:
          description: Nationality label in French.
          example: Française
          nullable: true
          type: string
      type: object
    FrenchNumeriquePhone:
      description: Phone number information including the full phone number and its
        structured components.
      properties:
        fullPhoneNumber:
          description: Full phone number including country prefix.
          example: "+33606060606"
          nullable: true
          type: string
        countryPrefix:
          description: International country calling code including the plus sign.
          example: "+33"
          nullable: true
          type: string
        nationalNumber:
          description: National phone number without the country prefix.
          example: "601020304"
          nullable: true
          type: string
      type: object
    FrenchNumeriqueProviderOutput:
      description: Exposed properties for the `france-identite-numerique` Provider
        which do not directly map to the normalized IdentityData model.
      properties:
        givenName:
          allOf:
          - $ref: '#/components/schemas/FrenchNumeriqueGivenName'
          description: |-
            Given name information including the full given name and its structured components
            (first name and middle name(s)).
          nullable: true
        familyName:
          description: |-
            Birth name (nom de naissance) as recorded on the user's French identity document.
            In France, this is the family name given at birth and may differ from the usage name.
          example: Dupont
          nullable: true
          type: string
        preferredSurname:
          description: |-
            Usage surname ("nom d'usage") - according to French law, this is the surname used in
            daily life. This is typically a married name or other preferred surname, as opposed to
            FamilyName which is the birth surname ("nom de naissance").
          example: Martin
          nullable: true
          type: string
        birthdate:
          description: |-
            Date of birth in YYYY-MM-DD format,
            as recorded on the user's French identity document.
          example: 1990-01-15
          nullable: true
          type: string
        nationality:
          allOf:
          - $ref: '#/components/schemas/FrenchNumeriqueNationality'
          description: |-
            Nationality information with ISO 3166 alpha-3 code and French label.
            The label is provided in French (e.g., "Française" for French nationality).
          example:
            code: FRA
            label: Française
          nullable: true
        sex:
          description: Sex - "Male" or "Female".
          example: Male
          nullable: true
          type: string
        majority:
          description: |-
            Majority status (majorité) indicating whether the user has reached the
            French age of legal majority (18 years old). This value is computed by
            the provider from the user's birthdate.
          example: true
          nullable: true
          type: boolean
        phoneNumber:
          allOf:
          - $ref: '#/components/schemas/FrenchNumeriquePhone'
          description: |-
            Phone number information including the full phone number and its structured components
            (country prefix and national number).
          nullable: true
        phoneNumberVerified:
          description: Whether the phone number has been verified by the provider.
          example: true
          nullable: true
          type: boolean
        email:
          description: Email address registered in the user's digital identity account.
          example: jean.dupont@example.fr
          nullable: true
          type: string
        emailVerified:
          description: Whether the email address has been verified by the provider.
          example: true
          nullable: true
          type: boolean
        birthplace:
          description: "National Institute of Statistics and Economic Studies (INSEE)\
            \ official geographic code (COG)\nof the birthplace. This is a 5-digit\
            \ French administrative code identifying the commune\n(municipality) of\
            \ birth. For births in metropolitan France, the first two digits represent\n\
            the department.\n            \nSee: https://www.insee.fr/fr/information/2560452"
          example: "75056"
          nullable: true
          type: string
        birthplaceLabel:
          description: |-
            Name of the birthplace commune (municipality). If applicable, this includes the full name of
            the arrondissement (borough).
          example: PARIS-9E-ARRONDISSEMENT
          nullable: true
          type: string
        birthCountry:
          description: "National Institute of Statistics and Economic Studies (INSEE)\
            \ official geographic code (COG)\nfor the user's country of birth.\n \
            \           \nLaPoste returns this as a 5-character numeric string. This\
            \ is a country/territory COG,\nso it always starts with \"99\" (99xxx).\
            \ France is coded as 99100.\n            \nSee full list here: https://www.insee.fr/fr/information/2560452"
          example: "99100"
          nullable: true
          type: string
        birthCountryIso:
          description: Birth country as an ISO 3166 alpha-3 code.
          example: FRA
          nullable: true
          type: string
        birthCountryLabel:
          description: "Label of the birth country, in English."
          example: France
          nullable: true
          type: string
        birthDepartment:
          description: "Number of the department of birth (e.g., \"75\" for Paris,\
            \ \"2A\" for Corse-du-Sud).\n            \nSee full list here: https://en.wikipedia.org/wiki/Departments_of_France"
          example: "75"
          nullable: true
          type: string
        digitalIdentityCreationDate:
          description: Date when the user's digital identity was created.
          example: 2023-06-15
          format: date
          nullable: true
          type: string
        digitalIdentityExpirationDate:
          description: |-
            Date when the digital identity expires. It is valid for 5 years from the date of identity verification.
            Users are notified ~1 month before and must re-verify identity via the app or in-person at La Poste.
            After expiration, the identity is deactivated but can be renewed within 1 year; otherwise deleted.
          example: 2028-06-15
          format: date
          nullable: true
          type: string
        identityDocumentType:
          description: |-
            Type of identity document used for verification.
            Can be "ID_CARD", "PASSPORT", or "RESIDENCE_PERMIT".
          example: PASSPORT
          nullable: true
          type: string
        identityDocumentNumber:
          description: "Identity document number as it appears on the ID Card, Passport\
            \ or Residence Permit."
          example: 51RF501150
          nullable: true
          type: string
        identityDocumentEmittingDate:
          description: Date when the identity document was issued.
          example: 2021-03-24
          format: date
          nullable: true
          type: string
        identityDocumentExpirationDate:
          description: Date when the identity document expires.
          example: 2031-03-24
          format: date
          nullable: true
          type: string
        identityDocumentEmittingCountry:
          description: Country that issued the identity document (ISO 3166 alpha-3
            code).
          example: FRA
          nullable: true
          type: string
        identityDocumentMrz:
          description: "Machine Readable Zone (MRZ) data from the ID Card, Passport\
            \ or Residence Permit."
          example: P<FRASPECIMEN<<NATACHA<<<<<<<<<<<<<<<<<<<<<<<<<51RF501150FRA7307122F2203242<<<<<<<<<<<<<<<02
          nullable: true
          type: string
      type: object
    GetAttachmentRequest:
      example:
        resultsAccessKey: resultsAccessKey
      properties:
        resultsAccessKey:
          description: "The Results Access Key for the Session associated with the\
            \ Attachment being retrieved.\n            \nThis is returned during Session\
            \ creation."
          minLength: 1
          type: string
      required:
      - resultsAccessKey
      type: object
    GetAttachmentResponse:
      example:
        contentType: contentType
        content: content
      properties:
        content:
          description: The raw file contents of the Attachment
          format: byte
          type: string
        contentType:
          description: The MIME type of the Attachment data
          type: string
      required:
      - content
      - contentType
      type: object
    GetSessionResponse:
      example:
        session: ""
      properties:
        session:
          allOf:
          - $ref: '#/components/schemas/Session'
      required:
      - session
      type: object
    GetSessionResultRequest:
      example:
        resultsAccessKey: resultsAccessKey
      properties:
        resultsAccessKey:
          description: The Results Access Key to exchange
          type: string
      required:
      - resultsAccessKey
      type: object
    GetSessionResultResponse:
      example:
        session: ""
        identityData: ""
      properties:
        session:
          allOf:
          - $ref: '#/components/schemas/Session'
        identityData:
          allOf:
          - $ref: '#/components/schemas/IdentityData'
          nullable: true
      required:
      - session
      type: object
    GoogleWalletInput:
      properties:
        exchangeMechanism:
          allOf:
          - $ref: '#/components/schemas/MdlExchangeMechanism'
          description: "The exchange mechanism to use for this Google Wallet verification.\n\
            \            \nUse `DigitalCredentialsApi` for Digital Credentials API\
            \ on web, or `NativeApp` for a native Android app."
          nullable: true
      type: object
    GoogleWalletProviderOutput:
      description: Exposed properties for the `google-wallet` Provider which do not
        directly map to the normalized IdentityData model.
      properties:
        rawMdlOutput:
          allOf:
          - $ref: '#/components/schemas/MdlOutput'
          description: The raw output of the mDL exchange performed through Google
            Wallet.
      required:
      - rawMdlOutput
      type: object
    HttpValidationProblemDetails:
      allOf:
      - $ref: '#/components/schemas/ProblemDetails'
      - additionalProperties: {}
        properties:
          errors:
            additionalProperties:
              items:
                type: string
              type: array
            type: object
        type: object
    IdentityData:
      properties:
        originatingProviderId:
          description: "The ID of the provider from which this data originated (eg\
            \ \"yoti\", \"clear\")"
          nullable: true
          type: string
        originatingSubProviderId:
          description: "The sub-provider ID of the provider from which this data originated\
            \ (eg \"rabo\", \"poste-italiane\")\n            \nThis is applicable\
            \ only to federated Identity Providers such as SPID and IDIN."
          nullable: true
          type: string
        person:
          allOf:
          - $ref: '#/components/schemas/PersonData'
          description: Identity data of the individual who was verified
          nullable: true
        document:
          allOf:
          - $ref: '#/components/schemas/DocumentData'
          description: "Identity data of the document involved in verification, if\
            \ relevant"
          nullable: true
        match:
          allOf:
          - $ref: '#/components/schemas/MatchData'
          description: "Match results for the data being matched against.\n      \
            \      \nThis applies to Providers which operate based on matching data\
            \ / biometrics against a government database,\nreturning match scores\
            \ or results as opposed to the data itself."
          nullable: true
        attachments:
          description: "Information for each attachment included with this set of\
            \ identity data.\n            \nUse the Attachments API to fetch an attachment\
            \ by its ID for a given Session."
          items:
            $ref: '#/components/schemas/AttachmentInfo'
          type: array
        providerOutput:
          allOf:
          - $ref: '#/components/schemas/ProviderOutput'
          description: "Provider-specific output data that doesn't fit the standard\
            \ identity data schema.\n            \nThe structure of this object varies\
            \ by provider."
          nullable: true
      required:
      - attachments
      type: object
    IdinInput:
      properties:
        subProviderId:
          description: "The ID of the specific bank to invoke with IDIN.\n       \
            \     \nIf not specified, the user will be prompted to select a bank."
          nullable: true
          type: string
      type: object
    IndonesiaDukcapilMatchInput:
      properties:
        fullName:
          description: The user's full name
          nullable: true
          type: string
        dateOfBirth:
          description: "The user's date of birth, in `YYYY-MM-DD` format"
          format: date
          nullable: true
          type: string
        nikIdNumber:
          description: The user's Indonesia NIK ID number
          nullable: true
          type: string
        email:
          description: "The email address of the individual.\n            \nEither\
            \ email or phone number must be provided."
          nullable: true
          type: string
        phoneNumber:
          description: "The phone number of the individual.\n            \nEither\
            \ email or phone number must be provided."
          nullable: true
          type: string
        selfieImage:
          description: "The raw bytes of the selfie image of the individual.\n   \
            \         \nMust be JPEG or PNG format; 10MB maximum."
          format: byte
          nullable: true
          type: string
        documentImage:
          description: "The raw bytes of the image of the individual's KTP government\
            \ ID.\n            \nMust be JPEG format; 1MB maximum.\n            \n\
            Optional."
          format: byte
          nullable: true
          type: string
        consentGivenAt:
          description: The timestamp when consent was given by the user for the verification.
          format: date-time
          nullable: true
          type: string
      type: object
    IndonesiaNikInput:
      properties:
        fullName:
          description: The user's full name
          minLength: 1
          type: string
        dateOfBirth:
          description: "The user's date of birth, in `YYYY-MM-DD` format"
          format: date
          type: string
        nikIdNumber:
          description: The user's Indonesia NIK ID number
          maxLength: 16
          minLength: 16
          pattern: ^\d*$
          type: string
      required:
      - dateOfBirth
      - fullName
      - nikIdNumber
      type: object
    IntegrationCapability:
      enum:
      - LaunchBrowser
      - DeeplinkToMobile
      - ShowContent
      - RefreshStepContent
      - CaptureRedirect
      - PollResult
      - PollAfterRedirect
      - NativeChallenge
      type: string
      x-enumDescriptions:
        LaunchBrowser: Signals that your integration supports the `LaunchBrowser`
          step type.
        DeeplinkToMobile: Signals that your integration supports the `DeeplinkToMobile`
          step type.
        ShowContent: Signals that your integration supports the `ShowContent` step
          type.
        RefreshStepContent: "Signals that your integration supports refreshable step\
          \ content, e.g., time-limited mobile deeplinks."
        CaptureRedirect: Signals that your integration supports the `CaptureRedirect`
          results collection method.
        PollResult: Signals that your integration supports the `PollResult` results
          collection method.
        PollAfterRedirect: "Signals that your integration supports polling after the\
          \ user has been redirected back to your application.\n            \nThis\
          \ is necessary for Providers which make results available asynchronously,\
          \ where the user is redirected back to your application before the results\
          \ are available."
        NativeChallenge: "Signals that your integration supports the `PerformNativeChallenge`\
          \ step type, paired with the `SubmitNativeChallengeResponse` collection\
          \ method.\n            \n`NativeChallenge` currently refers only to a native\
          \ mDL exchange API, such as Digital Credentials API on web\nor the Android\
          \ / iOS native app equivalents."
    IntegrationLaunchMethod:
      enum:
      - LaunchBrowser
      - None
      - DeeplinkToMobile
      - ShowContent
      - PerformNativeChallenge
      type: string
      x-enumDescriptions:
        LaunchBrowser: "The user's browser should be sent to the provided URL, either\
          \ in a new tab, pop-up, or top-level navigation."
        None: |-
          No action needs to be taken by the user; simply poll with the provided `resultsAccessKey`
          for the results of the session.
        DeeplinkToMobile: "The user must be sent to the provided URL, which is a mobile\
          \ deeplink.\n            \nThe URL should be shown as a button on mobile,\
          \ or as a QR code on desktop."
        ShowContent: Content must be displayed to the user to guide them through the
          process of verifying their identity.
        PerformNativeChallenge: "A challenge must be sent to a native API on the user's\
          \ device, such as the Digital Credentials API on web,\nor the equivalent\
          \ mDL exchange APIs on Android or iOS.\n            \nUse Trinsic's UI SDKs\
          \ on Web, Android, or iOS to streamline this process."
    IntegrationStep:
      description: A step to perform in the process of executing an Direct Provider
        Session
      properties:
        method:
          allOf:
          - $ref: '#/components/schemas/IntegrationLaunchMethod'
          description: The launch method to perform
          example: LaunchBrowser
          x-enumDescriptions:
            LaunchBrowser: "The user's browser should be sent to the provided URL,\
              \ either in a new tab, pop-up, or top-level navigation."
            None: |-
              No action needs to be taken by the user; simply poll with the provided `resultsAccessKey`
              for the results of the session.
            DeeplinkToMobile: "The user must be sent to the provided URL, which is\
              \ a mobile deeplink.\n            \nThe URL should be shown as a button\
              \ on mobile, or as a QR code on desktop."
            ShowContent: Content must be displayed to the user to guide them through
              the process of verifying their identity.
            PerformNativeChallenge: "A challenge must be sent to a native API on the\
              \ user's device, such as the Digital Credentials API on web,\nor the\
              \ equivalent mDL exchange APIs on Android or iOS.\n            \nUse\
              \ Trinsic's UI SDKs on Web, Android, or iOS to streamline this process."
        content:
          description: "Step type-specific content related to the step: a URL for\
            \ `LaunchBrowser`, a deeplink for `DeeplinkToMobile` or a string to show\
            \ to the user for `ShowContent`."
          example: https://verify.trinsic.id/api/session/launch?clientToken=...
          type: string
        refresh:
          allOf:
          - $ref: '#/components/schemas/StepRefreshInfo'
          description: "If non-null, contains metadata about how to refresh the value\
            \ of `content`."
          nullable: true
      required:
      - content
      - method
      type: object
    ItsmeProviderOutput:
      description: Exposed properties for the `a-itsme-login` Provider which do not
        directly map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name of the verified individual
          example: John
          type: string
        lastName:
          description: The last name of the verified individual
          example: Doe
          type: string
        dateOfBirth:
          description: The date of birth of the verified individual
          example: 1985-12-10
          format: date
          type: string
        hashedNationalRegisterNumber:
          description: "The hashed version of the Belgian National Register Number\
            \ of the verified individual.\n            \nBy default, itsme does not\
            \ return the raw National Register Number of the individual; instead,\
            \ only a hashed version is returned.\n            \nYour account must\
            \ be approved by itsme to receive the raw, unhashed National Register\
            \ Number."
          example: 5d17e158243dff0bd79fbfd06b27b80d04a40a6a0425e1bbb474c7634a2918f7
          nullable: true
          type: string
        nationalRegisterNumber:
          description: "The raw (not hashed) Belgian National Register Number (\"\
            Rijksregisternummer\") of the verified individual.\n            \nOnly\
            \ returned if your account has been explicitly authorized to receive it\
            \ by itsme; by law, this data is considered sensitive personal data.\n\
            \            \nThis is an 11-digit number in the format YYMMDDXXXCC, where:\n\
            - YYMMDD represents the individual's date of birth (year, month, day).\n\
            - XXX is a sequential birth number, odd for females and even for males.\n\
            - CC is a checksum, calculated with the equation: 97 - (YYMMDDXXX mod\
            \ 97)\n            \nFor births in the year 2000 or later, the digit '2'\
            \ is prepended to the first 9 digits during checksum calculation."
          example: "83122412413"
          nullable: true
          type: string
      required:
      - dateOfBirth
      - firstName
      - lastName
      type: object
    KenyaNidBiometric2Input:
      properties:
        idNumber:
          description: The user's Kenyan National ID number
          nullable: true
          type: string
        livenessImages:
          description: |-
            An array of exactly 8 images required for biometric liveness verification.
            The first 7 images should be liveness frames captured during the liveness detection process,
            and the last image (8th) should be a selfie of the user. All images must be in JPEG format
            and each image must be less than 15MB in size.
          items:
            format: byte
            type: string
          nullable: true
          type: array
        testSelfie:
          description: "Test selfie for test environment (optional, maximum 15MB).\
            \ Must be JPEG format."
          format: byte
          nullable: true
          type: string
      type: object
    KenyaNidInput:
      properties:
        firstName:
          description: The user's first name as it appears in their National ID
          minLength: 1
          type: string
        middleName:
          description: The user's middle name as it appears in their National ID (optional)
          nullable: true
          type: string
        lastName:
          description: The user's middle name as it appears in their National ID (optional)
          minLength: 1
          type: string
        dateOfBirth:
          description: "The user's date of birth, in `YYYY-MM-DD` format"
          format: date
          type: string
        gender:
          description: "The user's gender as it appears in their National ID (e.g.,\
            \ male, female)"
          minLength: 1
          type: string
        nationalIdNumber:
          description: The user's Kenya National ID number
          maxLength: 8
          minLength: 8
          pattern: ^\d*$
          type: string
      required:
      - dateOfBirth
      - firstName
      - gender
      - lastName
      - nationalIdNumber
      type: object
    KenyaNidLookup2Address:
      description: "Represents a Kenya address as stored in the IPRS (Integrated Population\
        \ Registration System) database.\n            \nThe structure reflects the\
        \ pre-2010 provincial administration system: District > Division > Location.\n\
        Since the 2010 constitutional change to counties, these legacy administrative\
        \ divisions are still\nmaintained in IPRS records for historical continuity.\n\
        \            \nThe raw address from IPRS is a newline-separated string with\
        \ labeled administrative components.\nExample format:\n            \n```\n\
        P O BOX 41842 NAIROBI\nPARLIAMENT BUILDINGS\nLOCATION - CITY SQUARE\nDIVISION\
        \ - CENTRAL\nDISTRICT - NAIROBI\n```\n            \nThe first lines contain\
        \ informal address details (village, P.O. Box) while the suffixed lines\n\
        contain official administrative designations prefixed with labels like \"\
        LOCATION -\", \"DIVISION -\", \"DISTRICT -\"."
      properties:
        district:
          description: "The district where the person resides.\n            \nThis\
            \ is the highest level of the pre-2010 administrative hierarchy stored\
            \ in IPRS. Districts were\nthe primary administrative units under Kenya's\
            \ provincial system before the change to counties in 2010.\nDistricts\
            \ roughly correspond to sub-counties in the current administrative structure."
          example: NAIROBI
          nullable: true
          type: string
        division:
          description: "The division within the district.\n            \nThis is the\
            \ second level of the pre-2010 administrative hierarchy. Divisions were\
            \ administrative\nunits between districts and locations."
          example: CENTRAL
          nullable: true
          type: string
        location:
          description: "The location within the division.\n            \nThis is the\
            \ third level of the pre-2010 administrative hierarchy. Locations were\
            \ the smallest\nadministrative units."
          example: CITY SQUARE
          nullable: true
          type: string
        additionalLines:
          description: "Additional address lines that appear before the structured\
            \ administrative parts.\n            \nMay include P.O. Box (format: \"\
            P O BOX [number] [location]\"), village names, estate names,\nor other\
            \ informal locality information."
          example:
          - P O BOX 41842 NAIROBI
          - PARLIAMENT BUILDINGS
          items:
            type: string
          nullable: true
          type: array
        raw:
          description: "The raw, unparsed address string as returned from IPRS (Integrated\
            \ Population Registration System).\n            \nThis preserves the original\
            \ newline-separated format containing both informal address components\n\
            (village, P.O. Box) and labeled administrative divisions (LOCATION, DIVISION,\
            \ DISTRICT)."
          example: P O BOX 41842 NAIROBI\nPARLIAMENT BUILDINGS\nLOCATION - CITY SQUARE\nDIVISION
            - CENTRAL\nDISTRICT - NAIROBI
          nullable: true
          type: string
      type: object
    KenyaNidLookup2Input:
      properties:
        idNumber:
          description: The user's National ID number
          nullable: true
          type: string
      type: object
    KenyaNidLookup2ProviderOutput:
      description: Exposed properties for the `kenya-nid-lookup-2` Provider which
        do not directly map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name (given name) of the ID holder as recorded in
            IPRS.
          example: JUMA
          type: string
        surname:
          description: The surname (family name) of the ID holder as recorded in IPRS.
          example: ODHIAMBO
          type: string
        otherName:
          description: The other name (middle name) of the ID holder as recorded in
            IPRS.
          example: OTIENO
          nullable: true
          type: string
        sex:
          description: "The sex of the ID holder as recorded on the National ID.\n\
            \            \nPossible values:\n- Male\n- Female"
          example: Male
          type: string
        dateOfBirth:
          description: The date of birth of the ID holder as recorded in IPRS.
          example: 1985-01-01
          format: date
          nullable: true
          type: string
        citizenship:
          description: "Citizenship status as recorded in the IPRS civil registry\
            \ database.\n            \nFor the Kenya National ID lookup, this value\
            \ will always be \"Kenyan\" as the National ID is only\nissued to Kenyan\
            \ citizens. Non-citizens residing in Kenya are issued different identification\n\
            documents (Alien ID cards, refugee documentation, etc.) which are not\
            \ supported by this provider."
          example: Kenyan
          type: string
        idNumber:
          description: "The Kenya National ID Number (Nambari ya Kitambulisho).\n\
            \            \nThis is the primary unique identifier for Kenyan citizens\
            \ in all government systems, issued by\nthe National Registration Bureau\
            \ (NRB). The format is 1 to 9 numeric digits (regex: /^[0-9]{1,9}$/).\n\
            This number is assigned upon first registration at age 18 (or upon naturalization\
            \ for new citizens)\nand remains constant for life."
          example: "12345678"
          type: string
        serialNumber:
          description: "The physical card serial number printed on the Kenya National\
            \ ID card.\n            \nThis is distinct from the ID Number and serves\
            \ as a card issuance tracking identifier maintained\nby IPRS. This value\
            \ changes each time a new physical card is issued (loss, damage, renewal)."
          example: "98765432"
          type: string
        dateOfIssue:
          description: The date the National ID was issued by the National Registration
            Bureau (NRB).
          example: 2010-06-15
          format: date
          nullable: true
          type: string
        placeOfBirth:
          allOf:
          - $ref: '#/components/schemas/KenyaNidLookup2Address'
          description: "Place of birth as recorded in Kenya's civil registry (IPRS).\n\
            \            \nThis is structured according to Kenya's pre-2010 administrative\
            \ hierarchy (District > Division > Location)."
          example:
            district: NAIROBI
            division: CENTRAL
            location: NGARA
            additionalLines:
            - KENYATTA NATIONAL HOSPITAL
            raw: |-
              KENYATTA NATIONAL HOSPITAL
              LOCATION - NGARA
              DIVISION - CENTRAL
              DISTRICT - NAIROBI
          nullable: true
        placeOfResidence:
          allOf:
          - $ref: '#/components/schemas/KenyaNidLookup2Address'
          description: "Current residence address as registered in IPRS.\n       \
            \     \nThis represents the address on file at the time of ID registration\
            \ or last update, structured\naccording to Kenya's pre-2010 administrative\
            \ hierarchy (District > Division > Location)."
          example:
            district: NAIROBI
            division: CENTRAL
            location: CITY SQUARE
            additionalLines:
            - P O BOX 41842 NAIROBI
            - PARLIAMENT BUILDINGS
            raw: |-
              P O BOX 41842 NAIROBI
              PARLIAMENT BUILDINGS
              LOCATION - CITY SQUARE
              DIVISION - CENTRAL
              DISTRICT - NAIROBI
          nullable: true
      required:
      - citizenship
      - firstName
      - idNumber
      - serialNumber
      - sex
      - surname
      type: object
    KenyaNidMatch2Input:
      properties:
        idNumber:
          description: The user's Kenyan National ID number
          nullable: true
          type: string
        givenName:
          description: The user's first name as it appears in their National ID
          nullable: true
          type: string
        familyName:
          description: The user's last name as it appears in their National ID
          nullable: true
          type: string
        middleName:
          description: The user's middle name as it appears in their National ID (optional)
          nullable: true
          type: string
        dateOfBirth:
          description: "The user's date of birth, in `YYYY-MM-DD` format"
          format: date
          nullable: true
          type: string
        sex:
          allOf:
          - $ref: '#/components/schemas/KenyaNidMatch2InputSex'
          description: The user's sex as it appears in their National ID
          nullable: true
      type: object
    KenyaNidMatch2InputSex:
      enum:
      - Male
      - Female
      type: string
      x-enumDescriptions:
        Male: ""
        Female: ""
    LaWalletProviderOutput:
      description: Exposed properties for the `usa-louisiana-wallet` Provider which
        do not directly map to the normalized IdentityData model.
      properties:
        driversLicenseNumber:
          description: The number of the driver's license used to create the LA Wallet
            credential
          example: "001234567"
          type: string
        issueDate:
          description: The issue date of the driver's license used to create the LA
            Wallet credential
          example: 2020-06-30
          format: date
          type: string
        expirationDate:
          description: The expiration date of the driver's license used to create
            the LA Wallet credential
          example: 2030-06-30
          format: date
          type: string
        auditNumber:
          description: The 4-digit audit number of the driver's license used to create
            the LA Wallet credential
          example: "1234"
          type: string
        licenseStatus:
          description: The license status from the LA Wallet credential
          example: VALID
          type: string
        licenseClass:
          description: "The license class from the LA Wallet credential\n        \
            \    \nPossible values:\n- \"A\": Commercial Driver's License, Combination\
            \ Vehicles\n- \"B\": Commercial Driver's License, Heavy Straight Vehicle\n\
            - \"C\": Commercial Driver's License, Light Straight Vehicle\n- \"D\"\
            : Chauffeur's Driver's License\n- \"E\": Driver's License for Personal\
            \ Vehicle"
          example: E
          type: string
        firstName:
          description: The first name from the LA Wallet credential
          example: JOHN
          type: string
        middleName:
          description: The middle name from the LA Wallet credential
          example: JACOB
          type: string
        lastName:
          description: The last name from the LA Wallet credential
          example: DOE
          type: string
        dateOfBirth:
          description: The date of birth from the LA Wallet credential
          example: 1990-01-15
          format: date
          type: string
        sex:
          description: The sex from the LA Wallet credential
          example: Male
          type: string
        addressLine1:
          description: The address' line 1 from the LA Wallet credential
          example: 123 MAIN ST
          type: string
        addressLine2:
          description: The address' line 2 from the LA Wallet credential
          example: '#5'
          nullable: true
          type: string
        addressCity:
          description: The address' city from the LA Wallet credential
          example: BATON ROUGE
          type: string
        addressState:
          description: The address' state from the LA Wallet credential
          example: LA
          type: string
        addressZip:
          description: The address' ZIP from the LA Wallet credential
          example: "70801"
          type: string
        county:
          description: "The county (\"parish\") code from the LA Wallet credential.\n\
            \            \nThis is a number from 1 to 64, representing one of Louisiana's\
            \ 64 parishes."
          example: "17"
          type: string
        coarseAge:
          description: "The coarse age returned by LA Wallet for this credential\n\
            \            \nPossible values:\n- \"Under 18\"\n- \"Under 21\"\n- \"\
            Over 21\""
          example: Over 21
          type: string
      required:
      - addressCity
      - addressLine1
      - addressState
      - addressZip
      - auditNumber
      - coarseAge
      - county
      - dateOfBirth
      - driversLicenseNumber
      - expirationDate
      - firstName
      - issueDate
      - lastName
      - licenseClass
      - licenseStatus
      - middleName
      - sex
      type: object
    LatviaEparakstsMobileProviderOutput:
      description: Exposed properties for the `a-lv-eparaksts-mobile-login` Provider
        which do not directly map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name of the verified individual
          example: John
          type: string
        lastName:
          description: The last name of the verified individual
          example: Doe
          type: string
        personalCode:
          description: "The 11-digit Latvian personal code (\"personas kods\") of\
            \ the verified individual.\n            \nThis has two possible formats,\
            \ depending on whether the personal code was issued after July 1, 2017.\n\
            \            \nFor personal codes issued before July 1, 2017, the format\
            \ is DDMMYY-CZZZQ, where:\n- DDMMYY is the date of birth, followed by\
            \ an optional hyphen\n- C represents the century of birth ('0' for 1800-1899,\
            \ '1' for 1900-1999, '2' for 2000-2099)\n- ZZZ is a serial number\n- Q\
            \ is a checksum digit\n            \nFor personal codes issued on or after\
            \ July 1, 2017, the format is 32ZZZZZZZZQ, where:\n- 32 is a fixed prefix\n\
            - ZZZZZZZQ are 8 random digits\n- Q is a checksum digit\n            \n\
            NOTE: Individuals born before July 1, 2017 can elect to be issued a new\
            \ personal code which does not contain their birthdate.\nTherefore, no\
            \ concrete assumptions may be made about an individual's date of birth\
            \ based solely on the format of their personal code."
          example: 300680-15055
          type: string
      required:
      - firstName
      - lastName
      - personalCode
      type: object
    LatviaEparakstsProviderOutput:
      description: Exposed properties for the `a-lv-eparaksts-id-login` Provider which
        do not directly map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name of the verified individual
          example: John
          type: string
        lastName:
          description: The last name of the verified individual
          example: Doe
          type: string
        personalCode:
          description: "The 11-digit Latvian personal code (\"personas kods\") of\
            \ the verified individual.\n            \nThis has two possible formats,\
            \ depending on whether the personal code was issued after July 1, 2017.\n\
            \            \nFor personal codes issued before July 1, 2017, the format\
            \ is DDMMYY-CZZZQ, where:\n- DDMMYY is the date of birth, followed by\
            \ an optional hyphen\n- C represents the century of birth ('0' for 1800-1899,\
            \ '1' for 1900-1999, '2' for 2000-2099)\n- ZZZ is a serial number\n- Q\
            \ is a checksum digit\n            \nFor personal codes issued on or after\
            \ July 1, 2017, the format is 32ZZZZZZZZQ, where:\n- 32 is a fixed prefix\n\
            - ZZZZZZZQ are 8 random digits\n- Q is a checksum digit\n            \n\
            NOTE: Individuals born before July 1, 2017 can elect to be issued a new\
            \ personal code which does not contain their birthdate.\nTherefore, no\
            \ concrete assumptions may be made about an individual's date of birth\
            \ based solely on the format of their personal code."
          example: 300680-15055
          type: string
      required:
      - firstName
      - lastName
      - personalCode
      type: object
    ListProvidersResponse:
      example:
        providers:
        - collectionMethod: PollResult
          subtext: dmv.ca.gov
          regions:
          - Europe
          licensed: true
          hasTrinsicInterface: true
          description: Identity verification via government ID and biometrics
          requiresInput: false
          countries:
          - US
          - CA
          availableAttributes:
          - scope: person.givenName
            outputted: Always
          - scope: person.givenName
            outputted: Always
          launchMethod: DeeplinkToMobile
          logoUrl: https://cdn.trinsic.id/providers/yoti.png
          liveHealth: Offline
          testHealth: Offline
          geography:
          - Global
          name: Yoti
          resultsMayBeDelayedAfterRedirect: false
          supportsDirectProviderSessions: true
          hasRefreshableContent: false
          id: yoti
          availableAttachments:
          - type: document_portrait
            outputted: Always
          - type: document_portrait
            outputted: Always
          subProviders:
          - subtext: dmv.ca.gov
            name: name
            id: id
            logoUrl: logoUrl
          - subtext: dmv.ca.gov
            name: name
            id: id
            logoUrl: logoUrl
          subdivisions:
          - US-CA
          - US-TX
        - collectionMethod: PollResult
          subtext: dmv.ca.gov
          regions:
          - Europe
          licensed: true
          hasTrinsicInterface: true
          description: Identity verification via government ID and biometrics
          requiresInput: false
          countries:
          - US
          - CA
          availableAttributes:
          - scope: person.givenName
            outputted: Always
          - scope: person.givenName
            outputted: Always
          launchMethod: DeeplinkToMobile
          logoUrl: https://cdn.trinsic.id/providers/yoti.png
          liveHealth: Offline
          testHealth: Offline
          geography:
          - Global
          name: Yoti
          resultsMayBeDelayedAfterRedirect: false
          supportsDirectProviderSessions: true
          hasRefreshableContent: false
          id: yoti
          availableAttachments:
          - type: document_portrait
            outputted: Always
          - type: document_portrait
            outputted: Always
          subProviders:
          - subtext: dmv.ca.gov
            name: name
            id: id
            logoUrl: logoUrl
          - subtext: dmv.ca.gov
            name: name
            id: id
            logoUrl: logoUrl
          subdivisions:
          - US-CA
          - US-TX
      properties:
        providers:
          description: The list of providers available to this organization
          items:
            $ref: '#/components/schemas/Provider'
          type: array
      required:
      - providers
      type: object
    ListRedirectUrisResponse:
      description: Response from listing redirect URIs within an environment.
      example:
        uris:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          uri: uri
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          uri: uri
        more: true
      properties:
        uris:
          description: List of redirect uris within the environment.
          items:
            $ref: '#/components/schemas/RedirectUriResponse'
          type: array
        more:
          description: Whether there are additional pages of uris to retrieve
          example: true
          type: boolean
      required:
      - more
      - uris
      type: object
    ListSessionsResponse:
      example:
        sessions:
        - success: true
          created: 1722526411
          errorCode: ""
          id: 678c1ff6-2178-49e7-81d2-548afb6f85ec
          done: true
          updated: 1722526722
        - success: true
          created: 1722526411
          errorCode: ""
          id: 678c1ff6-2178-49e7-81d2-548afb6f85ec
          done: true
          updated: 1722526722
        total: 101
        more: true
      properties:
        sessions:
          items:
            $ref: '#/components/schemas/Session'
          type: array
        total:
          description: The total number of sessions tied to your account
          example: 101
          format: int32
          type: integer
        more:
          description: Whether there are additional pages of sessions to retrieve
          example: true
          type: boolean
      required:
      - more
      - sessions
      - total
      type: object
    ListVerificationProfilesResponse:
      example:
        verificationProfiles:
        - enabledProviders:
          - enabledProviders
          - enabledProviders
          brandName: brandName
          primaryColor: primaryColor
          alias: alias
          id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          isProductionUsage: true
          logoUrl: logoUrl
        - enabledProviders:
          - enabledProviders
          - enabledProviders
          brandName: brandName
          primaryColor: primaryColor
          alias: alias
          id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          isProductionUsage: true
          logoUrl: logoUrl
        more: true
      properties:
        verificationProfiles:
          items:
            $ref: '#/components/schemas/VerificationProfileResponse'
          type: array
        more:
          description: Whether there are additional pages of verification profiles
            to retrieve
          example: true
          type: boolean
      required:
      - more
      - verificationProfiles
      type: object
    LithuaniaIdCardProviderOutput:
      description: Exposed properties for the `a-lt-id-login` Provider which do not
        directly map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name of the verified individual
          example: John
          type: string
        lastName:
          description: The last name of the verified individual
          example: Doe
          type: string
        dateOfBirth:
          description: The date of birth of the verified individual
          example: 1985-12-10
          format: date
          type: string
        personalCode:
          description: "The 11-digit Lithuanian Personal Code (asmens kodas) of the\
            \ verified individual.\n            \nIf the first digit is \"9\" (rare),\
            \ the rest of the identifier is random and has no structure.\n       \
            \     \nOtherwise, this is in the format GYYMMDDSSSC, where:\n       \
            \     \n- G is both gender and century of birth\n- YYMMDD is the date\
            \ of birth\n- SSS is a sequential serial number\n- C is a checksum digit\n\
            \            \nIf G is odd, the gender is male.\nIf G is even, the gender\
            \ is female.\n            \nIf G is 1 or 2, the individual was born in\
            \ the 19th century (1800-1899).\nIf G is 3 or 4, the individual was born\
            \ in the 20th century (1900-1999).\nIf G is 5 or 6, the individual was\
            \ born in the 21st century (2000-2099).\n            \nRarely, the date\
            \ of birth may be encoded as 000000 if it is not known."
          example: "38006309999"
          type: string
      required:
      - dateOfBirth
      - firstName
      - lastName
      - personalCode
      type: object
    LloydsSmartIdProviderOutput:
      description: Exposed properties for the `lloyds-smart-id` Provider which do
        not directly map to the normalized IdentityData model.
      properties:
        rememberMeId:
          description: "The unique id for your service of the individual. This is\
            \ not shared between different services so if the user logs into a different\
            \ service, the user will have a new id for the other service."
          example: 9f63ff98-e1e3-433c-9b86-7c697d0b4e5f
          format: uuid
          nullable: true
          type: string
        email:
          description: The email address of the individual
          example: johndoe@example.com
          nullable: true
          type: string
        givenName:
          description: The given name of the individual. This can correspond to first
            and middle names in English.
          example: John Alvin
          nullable: true
          type: string
        familyName:
          description: The family name of the individual
          example: Doe
          nullable: true
          type: string
        fullName:
          description: |-
            Full name of the individual which is composed of the given and family name.
            Unverified full name may be safe to use. For example, Aadhaar card provides full name, but due to certain limitations of verifying Aadhaar, Yoti is unable to verify this property so it is listed as "unverified".
          example: John Alvin Doe
          nullable: true
          type: string
        dateOfBirth:
          description: |-
            The date of birth of the individual.
            Unverified date of birth may be safe to use. For example, Aadhaar card provides date of birth, but due to certain limitations of verifying Aadhaar, Yoti is unable to verify this property so it is listed as "unverified".
          example: 1985-01-31
          format: date
          nullable: true
          type: string
        gender:
          description: "The gender of the individual. Depending on the source or country,\
            \ gender may not be verified. This is a setting that can be configured\
            \ while setting up scopes.\n            \nUnverified gender may be safe\
            \ to use. For example, Aadhaar card provides gender, but due to certain\
            \ limitations of verifying Aadhaar, Yoti is unable to verify this property\
            \ so it is listed as \"unverified\".\nIn some countries\nPossible values:\n\
            - \"MALE\"\n- \"FEMALE\"\n- \"TRANSGENDER\"\n- \"OTHER\""
          example: MALE
          nullable: true
          type: string
        nationality:
          description: The nationality of the individual as a ISO alpha-3 code
          example: GBR
          nullable: true
          type: string
        mobileNumber:
          description: "The mobile phone number of the individual. This number was\
            \ verified with a one time password (OTP) during the user's registration\
            \ with Yoti. In some cases, Yoti may do additional checks against its\
            \ sources to confirm the user's identity."
          example: "+447777123456"
          nullable: true
          type: string
        structuredPostalAddress:
          allOf:
          - $ref: '#/components/schemas/YotiStructuredPostalAddress'
          description: |-
            A structured postal address for the individual that comes from the underlying document or can be manually added. In the case of being manually added, the address is considered "unverified".
            Unverified addresses may be safe to use. For example, Aadhaar card provides an address, but due to certain limitations of verifying Aadhaar, Yoti is unable to verify this property so it is listed as "unverified".
          nullable: true
        postalAddress:
          description: |-
            The whole address for the individual that comes from the underlying document or can be manually added. In the case of being manually added, the address is considered "unverified". The format of this will be different for each country.
            Refer to `StructuredPostalAddress` for a detailed version of the address.
          example: "6th Floor, 107 Leadenhall St, London, EC3A 4AF"
          nullable: true
          type: string
        documentDetails:
          allOf:
          - $ref: '#/components/schemas/YotiDocumentDetails'
          description: The details of the underlying document used to help create
            the Yoti credential
          nullable: true
      type: object
    Match:
      properties:
        probabilityValue:
          description: "The probability value of the match.\n            \nThis is\
            \ a value between 0 and 1, where 1 indicates a perfect match and 0 indicates\
            \ no match.\n            \nThis field is non-null when the underlying\
            \ Provider's match output is in the form of a probability score."
          format: double
          nullable: true
          type: number
        booleanValue:
          description: "The boolean value of the match.\n            \nThis is a value\
            \ of true or false, where true indicates a match and false indicates no\
            \ match.\n            \nThis field is non-null when the underlying Provider's\
            \ match output is in the form of a boolean value."
          nullable: true
          type: boolean
      type: object
    MatchData:
      description: "Match results for the data being matched against.\n          \
        \  \nThis applies to Providers which operate based on matching data / biometrics\
        \ against a government database,\nreturning match scores or results as opposed\
        \ to the data itself."
      properties:
        nationalIdNumber:
          allOf:
          - $ref: '#/components/schemas/Match'
          description: Whether the provided National ID Number matched the information
            on file for the individual
          example: true
          nullable: true
        fullName:
          allOf:
          - $ref: '#/components/schemas/Match'
          description: "The match score for the full name of the individual.\n   \
            \         \nHigher values indicate a closer match."
          example: 1
          nullable: true
        givenName:
          allOf:
          - $ref: '#/components/schemas/Match'
          description: "The match score for the given (first) name of the individual.\n\
            \            \nHigher values indicate a closer match."
          example: 1
          nullable: true
        middleName:
          allOf:
          - $ref: '#/components/schemas/Match'
          description: "The match score for the middle name(s) of the individual.\n\
            \            \nHigher values indicate a closer match."
          example: 1
          nullable: true
        familyName:
          allOf:
          - $ref: '#/components/schemas/Match'
          description: "The match score for the family (last) name of the individual.\n\
            \            \nHigher values indicate a closer match."
          example: 0
          nullable: true
        sex:
          allOf:
          - $ref: '#/components/schemas/Match'
          description: Whether the provided sex of the individual matched the information
            on file for the individual
          example: false
          nullable: true
        dateOfBirth:
          allOf:
          - $ref: '#/components/schemas/Match'
          description: Whether the provided date of birth matched the information
            on file for the individual
          example: true
          nullable: true
        phoneNumber:
          allOf:
          - $ref: '#/components/schemas/Match'
          description: Whether the provided phone number matched the information on
            file for the individual
          example: true
          nullable: true
        faceMatch:
          allOf:
          - $ref: '#/components/schemas/Match'
          description: "The match score for the face match between the provided selfie\
            \ image and the biometrics on file for the individual.\n            \n\
            Higher values indicate greater match confidence."
          example: 0.9646
          nullable: true
        liveness:
          allOf:
          - $ref: '#/components/schemas/Match'
          description: "The confidence score for the liveness check performed against\
            \ the selfie image of the individual.\n            \nHigher values indicate\
            \ lower suspicion."
          example: 0.9501
          nullable: true
        imageAuthenticity:
          allOf:
          - $ref: '#/components/schemas/Match'
          description: "The confidence score for the image manipulation check performed\
            \ against the selfie image of the individual.\n            \nHigher values\
            \ indicate lower suspicion of image manipulation."
          example: 0.2088
          nullable: true
      type: object
    MdlExchangeMechanism:
      enum:
      - NativeApp
      - DigitalCredentialsApi
      type: string
      x-enumDescriptions:
        NativeApp: "The mDL exchange will be performed using a native mobile app SDK\
          \ (e.g. Google Wallet, Apple Wallet)."
        DigitalCredentialsApi: The mDL exchange will be performed using the Digital
          Credentials API on web.
    MdlOutput:
      properties:
        iacaRootCertificate:
          allOf:
          - $ref: '#/components/schemas/MdlOutputCertificateData'
          description: Information about the IACA Root Certificate which signed (directly
            or indirectly) the Document Signer Certificate for this mDL.
          example:
            serialNumber: "535868140170056787981774412188464491855209169584"
            commonName: California DMV IACA Root
            stateOrProvinceName: US-CA
            notBefore: 2023-03-01T17:17:39.000Z
            notAfter: 2033-01-07T17:17:39.000Z
        documentSignerCertificate:
          allOf:
          - $ref: '#/components/schemas/MdlOutputCertificateData'
          description: Information about the Document Signer Certificate which signed
            the mDL presented by the user.
          example:
            serialNumber: "306801082360131207284784033026769989720690200468"
            commonName: California DMV IACA mdoc Signer
            stateOrProvinceName: US-CA
            notBefore: 2025-01-13T17:26:32.000Z
            notAfter: 2026-04-13T17:26:32.000Z
        documentType:
          description: "The document type of the mDL presented by the user.\n    \
            \        \nCommon values:\n- \"org.iso.18013.5.1.mDL\" for ISO 18013-5\
            \ mDLs\n- \"com.google.wallet.idcard.1\" for Google Wallet ID Cards"
          example: org.iso.18013.5.1.mDL
          type: string
        nameSpaces:
          additionalProperties:
            additionalProperties:
              $ref: '#/components/schemas/MdlOutputFieldData'
            type: object
          description: "The namespaces, and fields within those namespaces, which\
            \ were present in the processed mDL."
          example:
            org.iso.18013.5.1:
              family_name:
                type: String
                value: DOE
              given_name:
                type: String
                value: JOHN
              birth_date:
                type: String
                value: 2000-03-27
              age_over_21:
                type: Boolean
                value: "true"
          type: object
      required:
      - documentSignerCertificate
      - documentType
      - iacaRootCertificate
      - nameSpaces
      type: object
    MdlOutputCertificateData:
      properties:
        serialNumber:
          description: The serial number of the certificate
          example: "535868140170056787981774412188464491855209169584"
          type: string
        commonName:
          description: The common name (CN) of the certificate
          example: California DMV IACA Root
          type: string
        stateOrProvinceName:
          description: "The stateOrProvinceName field from the signing certificate.\n\
            \            \nPer the ISO 18013-5 (mDL) spec, this is an ISO 3166-2:2020\
            \ country subdivision code (e.g., \"US-CA\" for California, USA).\n  \
            \          \nMay be an empty string for certificates which are not state-specific\
            \ (e.g., Google Wallet's ID Pass certificates)."
          example: US-CA
          type: string
        notBefore:
          description: The date before which this certificate is not valid.
          example: 2023-03-01T17:17:39Z
          format: date-time
          type: string
        notAfter:
          description: The date after which this certificate is not valid.
          example: 2033-01-07T17:17:39Z
          format: date-time
          type: string
      required:
      - commonName
      - notAfter
      - notBefore
      - serialNumber
      - stateOrProvinceName
      type: object
    MdlOutputFieldData:
      properties:
        type:
          allOf:
          - $ref: '#/components/schemas/MdlOutputFieldDataType'
          description: The type of data contained in `value`.
          example: String
          x-enumDescriptions:
            String: "A raw string value, e.g. \"John\""
            Boolean: "A boolean value, e.g. \"true\""
            Number: "A numeric value, e.g. \"1234.56\", \"-1\", \"53\""
            Base64Url: "Base64url-encoded binary data, e.g. a JPEG image."
            EncodedCbor: "An encoded CBOR object which cannot be cleanly represented\
              \ as a simple string.\n            \nWhen this type is used, the `value`\
              \ field contains a base64url-encoded byte array of the raw CBOR data."
            IntentionallyOmitted: "Indicates that Trinsic intentionally omitted the\
              \ field's value from the raw mDL output.\n            \nThis is only\
              \ done for large field values which are already surfaced via Trinsic's\
              \ Attachments API, such as `portrait`.\n            \nThe presence of\
              \ this field indicates that the data was returned by the mDL, but has\
              \ been omitted to reduce payload size.\n            \nUse the Attachments\
              \ API to fetch this field."
        value:
          description: The string-encoded value of the field.
          example: 123 Fake St
          type: string
      required:
      - type
      - value
      type: object
    MdlOutputFieldDataType:
      enum:
      - String
      - Boolean
      - Number
      - Base64Url
      - EncodedCbor
      - IntentionallyOmitted
      type: string
      x-enumDescriptions:
        String: "A raw string value, e.g. \"John\""
        Boolean: "A boolean value, e.g. \"true\""
        Number: "A numeric value, e.g. \"1234.56\", \"-1\", \"53\""
        Base64Url: "Base64url-encoded binary data, e.g. a JPEG image."
        EncodedCbor: "An encoded CBOR object which cannot be cleanly represented as\
          \ a simple string.\n            \nWhen this type is used, the `value` field\
          \ contains a base64url-encoded byte array of the raw CBOR data."
        IntentionallyOmitted: "Indicates that Trinsic intentionally omitted the field's\
          \ value from the raw mDL output.\n            \nThis is only done for large\
          \ field values which are already surfaced via Trinsic's Attachments API,\
          \ such as `portrait`.\n            \nThe presence of this field indicates\
          \ that the data was returned by the mDL, but has been omitted to reduce\
          \ payload size.\n            \nUse the Attachments API to fetch this field."
    MexicoCurpInput:
      properties:
        curp:
          description: The user's CURP number
          maxLength: 18
          minLength: 18
          type: string
      required:
      - curp
      type: object
    MexicoCurpProviderOutput:
      description: Exposed properties for the `mexico-curp-lookup` Provider which
        do not directly map to the normalized IdentityData model.
      properties:
        curpStatus:
          description: "Curp status for the subject.\n            \nPossible values:\n\
            - AN: Alta Normal (Normal registration) - Active\n- AH: Alta con Homonimia\
            \ (Registration with homonymy) - Active\n- RCC: Registro de cambio afectando\
            \ a CURP (Change affecting CURP) - Active\n- RCN: Registro de cambio no\
            \ afectando a CURP (Change not affecting CURP) - Active\n- BAP: Baja por\
            \ documento apócrifo (Low due to apocryphal document) - Inactive\n- BSU:\
            \ Baja sin uso (Low curp without use) - Inactive\n- BD: Baja por defunció\
            n (Low curp due to death) - Inactive\n- BDM: Baja administrativa (Low,\
            \ due to administrative process) - Inactive\n- BDP: Baja por adopción\
            \ (Low, due to adoption) - Inactive\n- BJD: Baja Judicial (Low for judicial\
            \ reasons) - Inactive"
          example: AN
          nullable: true
          type: string
      type: object
    MitIdProviderOutput:
      description: Exposed properties for the `denmark-mitid` Provider which do not
        directly map to the normalized IdentityData model.
      properties:
        fullName:
          description: The full name of the individual.
          example: Ove Henriksen
          nullable: true
          type: string
        dateOfBirth:
          description: The date of birth of the individual.
          example: 1943-08-15
          format: date
          nullable: true
          type: string
        uniqueIdentifier:
          description: |-
            A stable unique identifier representing the user in MitID's system.
            It is recommended to use this identifier instead of using the civil registration number (CPR number) directly.
          example: 8ba191ec-64b1-41c1-b622-4b8c14c34428
          format: uuid
          nullable: true
          type: string
        authenticationAssuranceLevel:
          description: "The authentication assurance level (AAL) for the verification.\n\
            The National Institute of Standards and Technology (NIST) measures the\
            \ confidence in a digital identity's authentication strength set by a\
            \ set of requirements for different levels.\nMore information can be found\
            \ here: https://pages.nist.gov/800-63-4/sp800-63b/aal/#AAL_SEC4\nPossible\
            \ values:\n            \n-  https://data.gov.dk/concept/core/nsis/Low\
            \ - (Level 1) Provides basic confidence that the authenticator is either\
            \ single factor or multifactor authentication.\n- https://data.gov.dk/concept/core/nsis/Substantial\
            \ - (Level 2) Provides high confidence that the authenticator uses two\
            \ distinct authentication factors that use secure authentication protocols.\n\
            - https://data.gov.dk/concept/core/nsis/High - (Level 3) Provides very\
            \ high confidence that the authenticator uses an non-exportable private\
            \ key and another authentication factor.\n            \n**Note**: These\
            \ urls are not valid anymore, but they still contain the level of assurance\
            \ name."
          example: https://data.gov.dk/concept/core/nsis/Substantial
          nullable: true
          type: string
        identityAuthenticationLevel:
          description: "The identity assurance level (IAL) for the verification.\n\
            The National Institute of Standards and Technology (NIST) measures the\
            \ confidence of the digital identity's verification set by a set of requirements\
            \ for different levels.\nMore information can be found here: https://pages.nist.gov/800-63-4/sp800-63b/aal/#AAL_SEC4\n\
            Possible values:\n            \n-  https://data.gov.dk/concept/core/nsis/Low\
            \ - (Level 1) The user has self asserted their identity and is neither\
            \ verified nor validated.\n- https://data.gov.dk/concept/core/nsis/Substantial\
            \ - (Level 2) The user has performed either a remote or in-person identity\
            \ proofing.\n- https://data.gov.dk/concept/core/nsis/High - (Level 3)\
            \ The user has performed an in person identity proofing with an authorized\
            \ representative.\n            \n**Note**: These urls are not valid anymore,\
            \ but they still contain the level of assurance name."
          example: https://data.gov.dk/concept/core/nsis/Substantial
          nullable: true
          type: string
        levelOfAssurance:
          description: "The level of assurance (LOA) for the verification. This is\
            \ an older retired model that is a combination of Identity Assurance Level\
            \ and Authentication Assurance Level.\nThe National Institute of Standards\
            \ and Technology (NIST) measures the confidence of the digital identity's\
            \ verification and authentication strength by a set of requirements for\
            \ different levels.\n            \nPossible values:\n-  https://data.gov.dk/concept/core/nsis/Low\
            \ - (Level 1) The user has self asserted their identity and multifactor\
            \ authentication is not required.\n- https://data.gov.dk/concept/core/nsis/Substantial\
            \ - (Level 3) The user has performed either a remote or in-person identity\
            \ proofing and multifactor authentication is required.\n- https://data.gov.dk/concept/core/nsis/High\
            \ - (Level 4) The user has performed an in person identity proofing with\
            \ an authorized representative and has strong cryptographic authentication\
            \ requirements.\n            \n**Note**: These urls are not valid anymore,\
            \ but they still contain the level of assurance name. MitID only returns\
            \ three of the four potential levels and does not include level 2.\n \
            \           \nMore information can be found here:\n- https://pages.nist.gov/800-63-3/sp800-63-3.html\n\
            - https://pages.nist.gov/800-63-3/"
          example: https://data.gov.dk/concept/core/nsis/Substantial
          nullable: true
          type: string
        identificationSource:
          description: "The source of the identification for the verification\n  \
            \          \nPossible values:\n- private - The verification is from a\
            \ private individual.\n- professional - The verification is a user in\
            \ behalf of a organization."
          example: private
          nullable: true
          type: string
        civilRegistrationNumber:
          description: "The civil registration number (CPR number) of the individual\
            \ for the verification.\n            \nThe CPR number is a 10-digit number\
            \ in the format DDMMYYY-XXXX, where:\n- DDMMYYY represents the individual's\
            \ date of birth (day, month, year).\n- XXXX is a unique identifier.\n\
            \            \nIf XXXX is even, the individual is female.\nIf XXXX is\
            \ odd, the individual is male.\n            \nMore information can be\
            \ found here: https://international.kk.dk/live/cpr-registration-and-documents/cpr-number"
          example: 01311985-1234
          nullable: true
          type: string
        organizationName:
          description: The name of the organization for the verification
          example: Dansk Standard
          nullable: true
          type: string
        organizationNumber:
          description: The organization number (CVR number). This is an 8-digit unique
            identifier for the organization. This will be prefixed with "DK" for VAT
            numbers.
          example: DK12345678
          nullable: true
          type: string
        authorizedRepresentativeNumber:
          description: |-
            The organization number (CVR number) of the organization the user is authorized to represent.
            This is only returned if the requested scope is for an organization verification and the user provides a private source in behalf of a company.
          example: 8ba191ec-64b1-41c1-b622-4b8c14c34428
          nullable: true
          type: string
      type: object
    MobileIdInput:
      properties:
        mobileIdPhoneNumber:
          description: The user's phone number in E.164 format
          nullable: true
          type: string
        mobileIdNationalIdentityNumber:
          description: The user's National ID number
          nullable: true
          type: string
        mobileIdLanguage:
          allOf:
          - $ref: '#/components/schemas/MobileIdLanguage'
          description: "The user's language -- LIT, EST, ENG, or RUS"
          nullable: true
      type: object
    MobileIdLanguage:
      enum:
      - LIT
      - EST
      - ENG
      - RUS
      type: string
      x-enumDescriptions:
        LIT: Lithuanian
        EST: Estonian
        ENG: English
        RUS: Russian
    MobileIdProviderOutput:
      description: Exposed properties for the `mobile-id` Provider which do not directly
        map to the normalized IdentityData model.
      properties:
        givenName:
          description: |-
            The given name of the individual, extracted from the Mobile ID authentication
            certificate's Subject Distinguished Name "G" (givenName) attribute.
          example: MARI
          nullable: true
          type: string
        familyName:
          description: |-
            The family name (surname) of the individual, extracted from the Mobile ID
            authentication certificate's Subject Distinguished Name "SN" (surname) attribute.
          example: TAMM
          nullable: true
          type: string
        dateOfBirth:
          description: "The individual's date of birth, derived from the personal\
            \ code."
          example: 1985-01-01
          format: date
          nullable: true
          type: string
        sex:
          description: "The individual's sex, derived from the first digit of the\
            \ personal code.\n            \nPossible values:\n- Male\n- Female"
          example: Female
          nullable: true
          type: string
        country:
          description: "The ISO 3166-1 alpha-2 country code extracted from the Mobile\
            \ ID authentication\ncertificate's Subject Distinguished Name \"C\" (country)\
            \ attribute.\n            \nThis represents the country of the certificate\
            \ issuer, not the person's nationality\nor citizenship. For Mobile ID,\
            \ this will be \"EE\" (Estonia) or \"LT\" (Lithuania)."
          example: EE
          nullable: true
          type: string
        identityType:
          description: "The identity document type, extracted from the first 3 characters\
            \ of the\nSERIALNUMBER field (before the country code).\n            \n\
            Possible values:\n- PNO: Personal Number (national civic registration\
            \ number)\n- PAS: Passport\n- IDC: National identity card"
          example: PNO
          nullable: true
          type: string
        personalCode:
          description: "The personal code (Estonian: isikukood, Lithuanian: asmens\
            \ kodas) of the individual,\nextracted from the SERIALNUMBER field of\
            \ the Mobile ID authentication certificate.\nThis is the portion after\
            \ the identity type and country prefix\n(e.g., \"48501010014\" from \"\
            PNOEE-48501010014\").\n            \nBoth Estonian and Lithuanian personal\
            \ codes are 11 digits in the format GYYMMDDSSSC where:\n- G = century/gender\
            \ (3-4 = 1900s, 5-6 = 2000s; odd = male, even = female)\n- YYMMDD = date\
            \ of birth\n- SSS = sequence number\n- C = checksum digit\n          \
            \  \nSee:\n- https://en.wikipedia.org/wiki/National_identification_number#Estonia\n\
            - https://en.wikipedia.org/wiki/National_identification_number#Lithuania"
          example: "48501010014"
          nullable: true
          type: string
        serialNumber:
          description: "The SERIALNUMBER attribute from the Mobile ID authentication\
            \ certificate's Subject\nDistinguished Name. Format: \"{identity-type}{country-code}-{identifier}\"\
            \n            \nComponents:\n- identity-type (3 chars): PNO (Personal\
            \ Number), PAS (Passport), IDC (ID Card)\n- country-code (2 chars): ISO\
            \ 3166-1 alpha-2 (EE, LT)\n- identifier: The personal code"
          example: PNOEE-48501010014
          nullable: true
          type: string
        certificateSubject:
          description: |-
            The full Subject Distinguished Name (Subject DN) from the Mobile ID
            authentication certificate. Contains comma-separated RDN (Relative Distinguished Name)
            components including C (Country), CN (Common Name), SN (Surname), G (Given Name),
            and SERIALNUMBER (Personal identifier).
          example: "SERIALNUMBER=PNOEE-48501010014, G=MARI, SN=TAMM, CN=\"TAMM,MARI,48501010014\"\
            , C=EE"
          nullable: true
          type: string
      type: object
    NigeriaNinBiometric2Input:
      properties:
        idNumber:
          description: The user's Nigerian National ID number (NIN)
          nullable: true
          type: string
        livenessImages:
          description: |-
            An array of exactly 8 images required for biometric liveness verification.
            The first 7 images should be liveness frames captured during the liveness detection process,
            and the last image (8th) should be a selfie of the user. All images must be in JPEG format
            and each image must be less than 15MB in size.
          items:
            format: byte
            type: string
          nullable: true
          type: array
        testSelfie:
          description: "Test selfie for test environment (optional, maximum 15MB).\
            \ Must be JPEG format."
          format: byte
          nullable: true
          type: string
      type: object
    NigeriaNinInput:
      properties:
        firstName:
          description: The user's first name as it appears in their National ID
          minLength: 1
          type: string
        middleName:
          description: The user's middle name as it appears in their National ID (optional)
          nullable: true
          type: string
        lastName:
          description: The user's last name as it appears in their National ID (optional)
          minLength: 1
          type: string
        phoneNumber:
          description: The user's phone number (optional)
          nullable: true
          type: string
        dateOfBirth:
          description: "The user's date of birth, in `YYYY-MM-DD` format"
          format: date
          type: string
        gender:
          description: "The user's gender as it appears in their National ID (e.g.,\
            \ male, female)"
          nullable: true
          type: string
        nationalIdNumber:
          description: The user's National ID number
          maxLength: 11
          minLength: 11
          pattern: ^\d*$
          type: string
      required:
      - dateOfBirth
      - firstName
      - lastName
      - nationalIdNumber
      type: object
    NigeriaNinLookup2Input:
      properties:
        idNumber:
          description: The user's National ID number (NIN)
          nullable: true
          type: string
      type: object
    NigeriaNinMatch2Input:
      properties:
        idNumber:
          description: The user's Nigerian National ID number (NIN)
          nullable: true
          type: string
        givenName:
          description: The user's first name as it appears in their National ID
          nullable: true
          type: string
        familyName:
          description: The user's last name as it appears in their National ID
          nullable: true
          type: string
        middleName:
          description: The user's middle name as it appears in their National ID (optional)
          nullable: true
          type: string
        dateOfBirth:
          description: "The user's date of birth, in `YYYY-MM-DD` format"
          format: date
          nullable: true
          type: string
        sex:
          allOf:
          - $ref: '#/components/schemas/NigeriaNinMatch2InputSex'
          description: The user's sex as it appears in their National ID
          nullable: true
        phoneNumber:
          description: |-
            The user's phone number as it appears in their National ID (optional).
            Must be in E.164 international format: "+234XXXXXXXXX"
          nullable: true
          type: string
      type: object
    NigeriaNinMatch2InputSex:
      enum:
      - Male
      - Female
      type: string
      x-enumDescriptions:
        Male: ""
        Female: ""
    OrderDirection:
      enum:
      - Ascending
      - Descending
      type: string
      x-enumDescriptions:
        Ascending: ""
        Descending: ""
    PersonData:
      description: Identity information for the individual being verified
      properties:
        givenName:
          description: Given (first) name of the individual
          example: John
          nullable: true
          type: string
        familyName:
          description: Family (last) name of the individual
          example: Doe
          nullable: true
          type: string
        middleName:
          description: Middle name of the individual
          example: Jacob
          nullable: true
          type: string
        fullName:
          description: "The individual's full name as a single string.\n         \
            \   \nUseful for names which do not fit into a \"first middle last\" structure."
          example: John Jacob de Doe
          nullable: true
          type: string
        suffix:
          description: Suffix of the individual's name
          nullable: true
          type: string
        nationality:
          nullable: true
          type: string
        sex:
          allOf:
          - $ref: '#/components/schemas/Sex'
          nullable: true
        phoneNumber:
          nullable: true
          type: string
        address:
          allOf:
          - $ref: '#/components/schemas/Address'
          description: Address information for an individual
          nullable: true
        dateOfBirth:
          format: date
          nullable: true
          type: string
      type: object
    PeruDniAddress:
      description: "Not a typical address. Does not include street or city. Instead,\
        \ includes region, province and district."
      properties:
        region:
          description: "Region in Peru (also known as department). This is the first\
            \ level subdivision in the country.\n            \nFormat:\n- All uppercase."
          example: CUSCO
          type: string
        province:
          description: "Province within the region. This is the second level subdivision\
            \ in the country.\n            \nFormat:\n- All uppercase."
          example: URUBAMBA
          type: string
        district:
          description: "District within the province. This is the third level subdivision\
            \ in the country.\n            \nFormat:\n- All uppercase."
          example: MACHUPICCHU
          type: string
      required:
      - district
      - province
      - region
      type: object
    PeruDniLookupInput:
      properties:
        documentNumber:
          description: "The user's DNI number (8 digits).\n            \nFormat:\n\
            - Must not include verification digit. On the DNI card, a ninth digit\
            \ appears after the first eight, with value\n  0-9 or A-K. This is not\
            \ included in the DNI number when verifying against Peru's database.\n\
            - Peru DNI is sometimes represented with dots. Though uncommon, if dots\
            \ are included, they will be sanitized."
          example: "19050215"
          nullable: true
          type: string
      type: object
    PeruDniProviderOutput:
      description: Exposed properties for the `peru-dni-lookup` Provider which do
        not directly map to the normalized IdentityData model.
      properties:
        address:
          allOf:
          - $ref: '#/components/schemas/PeruDniAddress'
          description: "Information about the general area in which the DNI holder\
            \ resides.\n            \nThis field is not always available."
          nullable: true
        arrayName:
          description: "All names that appear on DNI, as an array of strings.\n  \
            \          \nFormat:\n- All uppercase\n- Ordered by paternal family name,\
            \ then maternal family name, then given names."
          example:
          - QUISPE
          - MAMANI
          - JUAN
          - CARLOS
          items:
            type: string
          type: array
        civilStatus:
          description: "Marital status as it appears on the DNI.\n            \nThis\
            \ field is not always available.\n            \nValid values:\n- \"Single\"\
            \n- \"Married\"\n- \"Divorced\"\n- \"Widowed\""
          example: Single
          nullable: true
          type: string
        dateOfBirth:
          description: "Date of birth as it appears on the DNI.\n            \nThis\
            \ field is not always available."
          example: 1999-01-14
          format: date
          nullable: true
          type: string
        documentNumber:
          description: "The user's National Identity Document number (Documento Nacional\
            \ de Identidad or DNI).\n            \nFormat:\n- 8 digits\n- Does NOT\
            \ include verification digit. On the DNI card, a ninth digit appears next\
            \ to the first eight. It is\n  0-9 or A-K. This is NOT included in the\
            \ DNI number when returned from Peru's database.\n- Does NOT include dots\
            \ or hyphens"
          example: "19050215"
          type: string
        documentType:
          description: Should always be "DNI".
          example: DNI
          type: string
        expeditionDate:
          description: "Date that the DNI was issued.\n            \nThis field is\
            \ not always available.\n            \nFormat:\n- yyyy-MM-dd"
          example: 2025-01-14
          format: date
          nullable: true
          type: string
        expirationDate:
          description: "Date that the DNI will expire. DNI expires every 8 years,\
            \ unless the citizen is >= 60 years old, in which case\nit never expires.\n\
            \            \nThis field is not always available.\n            \nFormat:\n\
            - yyyy-MM-dd"
          example: 2033-01-14
          format: date
          nullable: true
          type: string
        firstName:
          description: "Given names as they appear on DNI.\n            \nFormat:\n\
            - All uppercase\n- Space-separated\n- Will include all given names"
          example: JUAN CARLOS
          type: string
        fullName:
          description: "All names as they appear on DNI.\n            \nFormat:\n\
            - All uppercase\n- Space-separated\n- Will include all names, given and\
            \ family\n- Ordered by given names first, then paternal family name, then\
            \ maternal family name"
          example: JUAN CARLOS QUISPE MAMANI
          type: string
        lastName:
          description: "Family names as they appear on DNI.\n            \nFormat:\n\
            - All uppercase\n- Will include all family names\n- Ordered by paternal\
            \ family name first, then maternal family name"
          example: QUISPE MAMANI
          type: string
        maternalLastName:
          description: "Maternal last name as it appears on DNI.\n            \nFormat:\n\
            - All uppercase"
          example: MAMANI
          type: string
        paternalLastName:
          description: "Paternal last name as it appears on DNI.\n            \nFormat:\n\
            - All uppercase"
          example: QUISPE
          type: string
        sex:
          description: "Sex as it appears on DNI.\n            \nThis field is not\
            \ always available.\n            \nValues:\n- \"Male\"\n- \"Female\""
          example: Male
          nullable: true
          type: string
        ubigeoReniec:
          description: "In Peru, geographical locations have an official geographical\
            \ code called UBIGEO, from the spanish \"UBIcación\nGEOgráfica\" (Geographic\
            \ Location). This is an administrative geocode, is different from a postal\
            \ code (which\nPeru also has) and is used to specifically delineate the\
            \ administrative region, province and district hierarchy.\n          \
            \  \nThere are two coding systems for UBIGEO: one from INEI (National\
            \ Institute of Statistics and Informatics) and\nanother from RENIEC (National\
            \ Registry of Identification and Civil Status). The two coding systems\
            \ are similar\nbut are not 100% the same (some numbers will map to different\
            \ geographic locations). This field follows the\ncoding system from RENIEC.\n\
            \            \nThis field is not always available.\n            \nFormat:\n\
            - Always 6 digits\n- First two digits represent region\n- Middle two are\
            \ province\n- Last two are district\n            \nGiven the example 081304,\
            \ that would correspond to:\n- 08 - Cusco Region\n- 0813 - Urubamba Province\n\
            - 081304 - Machupicchu District"
          example: "081304"
          nullable: true
          type: string
        verificationDigit:
          description: "The final (ninth) digit of the DNI, which serves as a checksum\
            \ over the first eight digits.\n            \nThis field is not always\
            \ available.\n            \nFormat:\n- Single character\n- Either 0-9\
            \ or A-K\n            \nRead more here:\n  https://elcomercio.pe/mag/respuestas/cual-es-el-digito-verificador-de-mi-dni-documento-nacional-de-identidad-reniec-peru-nnda-nnlt-noticia/"
          example: "7"
          nullable: true
          type: string
      required:
      - arrayName
      - documentNumber
      - documentType
      - firstName
      - fullName
      - lastName
      - maternalLastName
      - paternalLastName
      type: object
    PhilippineMatchInput:
      properties:
        givenName:
          description: The user's given / first name
          nullable: true
          type: string
        middleName:
          description: The user's middle name
          nullable: true
          type: string
        familyName:
          description: The user's family / last name
          nullable: true
          type: string
        suffix:
          description: The user's name suffix
          nullable: true
          type: string
        dateOfBirth:
          description: "The user's date of birth, in `YYYY-MM-DD` format"
          format: date
          nullable: true
          type: string
      type: object
    PhilippineQRInput:
      properties:
        qrCodeText:
          description: The raw text of the user's QR code after decoding it.
          nullable: true
          type: string
        qrCodeImage:
          description: The raw bytes of the image containing the user's QR code.
          format: byte
          nullable: true
          type: string
      type: object
    PhilippinesDigitalNidProviderOutput:
      description: Exposed properties for the `philippines-digital-national-id-qr`
        Provider which do not directly map to the normalized IdentityData model.
      properties:
        philsysCardNumber:
          description: |-
            The PhilSys Card Number (PCN). Every citizen or resident alien registered in PhilSys has a PhilSys Number (PSN). This number is tokenized into a card number to protect the PSN.
            The PhilSys Card Number is 12 characters long, and often is written in octets with dashes in between.
          example: 1234-5678-9101-1213
          type: string
      required:
      - philsysCardNumber
      type: object
    PhilippinesPhysicalNidProviderOutput:
      description: Exposed properties for the `philippines-physical-national-id-qr`
        Provider which do not directly map to the normalized IdentityData model.
      properties:
        philsysCardNumber:
          description: |-
            The PhilSys Card Number (PCN). Every citizen or resident alien registered in PhilSys has a PhilSys Number (PSN). This number is tokenized into a card number to protect the PSN.
            The PhilSys Card Number is 12 characters long, and often is written in octets with dashes in between.
          example: 1234-5678-9101-1213
          type: string
      required:
      - philsysCardNumber
      type: object
    PolandEdoAppProviderOutput:
      description: Exposed properties for the `a-edoapp-eid-login` Provider which
        do not directly map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name of the verified individual
          example: John
          type: string
        lastName:
          description: The last name of the verified individual
          example: Doe
          type: string
        dateOfBirth:
          description: The date of birth of the verified individual
          example: 1985-12-10
          format: date
          type: string
        nationalIdentificationNumber:
          description: "The 11-digit Polish national identification number (PESEL)\
            \ of the verified individual.\n            \nThis is in the format YYMMDDZZZGQ,\
            \ where:\n- YYMMDD is the date of birth\n- ZZZ is a unique identifier\n\
            - G is sex (even for females, odd for males)\n- Q is a checksum digit\n\
            \            \nThe year of birth encoded in this identifier assumes a\
            \ default year of birth in the 20th century.\nIf the year of birth is\
            \ in the range [1800, 1899], the month portion is incremented by 80.\n\
            If the year of birth is in the range [2000, 2099] the month portion is\
            \ incremented by 20.\nIf the year of birth is in the range [2100, 2199],\
            \ the month portion is incremented by 40.\nIf the year of birth is in\
            \ the range [2200, 2299], the month portion is incremented by 60."
          example: "90063014513"
          type: string
      required:
      - dateOfBirth
      - firstName
      - lastName
      - nationalIdentificationNumber
      type: object
    PolandMojeIdProviderOutput:
      description: Exposed properties for the `a-pl-mojeid-login` Provider which do
        not directly map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name of the verified individual
          example: John
          type: string
        lastName:
          description: The last name of the verified individual
          example: Doe
          type: string
        nationalIdentificationNumber:
          description: "The 11-digit Polish national identification number (PESEL)\
            \ of the verified individual.\n            \nThis is in the format YYMMDDZZZGQ,\
            \ where:\n- YYMMDD is the date of birth\n- ZZZ is a unique identifier\n\
            - G is sex (even for females, odd for males)\n- Q is a checksum digit\n\
            \            \nThe year of birth encoded in this identifier assumes a\
            \ default year of birth in the 20th century.\nIf the year of birth is\
            \ in the range [1800, 1899], the month portion is incremented by 80.\n\
            If the year of birth is in the range [2000, 2099] the month portion is\
            \ incremented by 20.\nIf the year of birth is in the range [2100, 2199],\
            \ the month portion is incremented by 40.\nIf the year of birth is in\
            \ the range [2200, 2299], the month portion is incremented by 60."
          example: "90063014513"
          type: string
      required:
      - firstName
      - lastName
      - nationalIdentificationNumber
      type: object
    PortugalIdCardProviderOutput:
      description: Exposed properties for the `a-pt-id-login` Provider which do not
        directly map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name of the verified individual
          example: John
          type: string
        lastName:
          description: The last name of the verified individual
          example: Doe
          type: string
        dateOfBirth:
          description: The date of birth of the verified individual
          example: 1985-12-10
          format: date
          type: string
        civilIdentificationNumber:
          description: The 8-digit Portuguese Civil Identification Number (número
            de identificação civil) of the verified individual.
          example: "12345678"
          type: string
      required:
      - civilIdentificationNumber
      - dateOfBirth
      - firstName
      - lastName
      type: object
    PostOfficeEasyIdProviderOutput:
      description: Exposed properties for the `post-office-easyid` Provider which
        do not directly map to the normalized IdentityData model.
      properties:
        rememberMeId:
          description: "The unique id for your service of the individual. This is\
            \ not shared between different services so if the user logs into a different\
            \ service, the user will have a new id for the other service."
          example: 9f63ff98-e1e3-433c-9b86-7c697d0b4e5f
          format: uuid
          nullable: true
          type: string
        email:
          description: The email address of the individual
          example: johndoe@example.com
          nullable: true
          type: string
        givenName:
          description: The given name of the individual. This can correspond to first
            and middle names in English.
          example: John Alvin
          nullable: true
          type: string
        familyName:
          description: The family name of the individual
          example: Doe
          nullable: true
          type: string
        fullName:
          description: |-
            Full name of the individual which is composed of the given and family name.
            Unverified full name may be safe to use. For example, Aadhaar card provides full name, but due to certain limitations of verifying Aadhaar, Yoti is unable to verify this property so it is listed as "unverified".
          example: John Alvin Doe
          nullable: true
          type: string
        dateOfBirth:
          description: |-
            The date of birth of the individual.
            Unverified date of birth may be safe to use. For example, Aadhaar card provides date of birth, but due to certain limitations of verifying Aadhaar, Yoti is unable to verify this property so it is listed as "unverified".
          example: 1985-01-31
          format: date
          nullable: true
          type: string
        gender:
          description: "The gender of the individual. Depending on the source or country,\
            \ gender may not be verified. This is a setting that can be configured\
            \ while setting up scopes.\n            \nUnverified gender may be safe\
            \ to use. For example, Aadhaar card provides gender, but due to certain\
            \ limitations of verifying Aadhaar, Yoti is unable to verify this property\
            \ so it is listed as \"unverified\".\nIn some countries\nPossible values:\n\
            - \"MALE\"\n- \"FEMALE\"\n- \"TRANSGENDER\"\n- \"OTHER\""
          example: MALE
          nullable: true
          type: string
        nationality:
          description: The nationality of the individual as a ISO alpha-3 code
          example: GBR
          nullable: true
          type: string
        mobileNumber:
          description: "The mobile phone number of the individual. This number was\
            \ verified with a one time password (OTP) during the user's registration\
            \ with Yoti. In some cases, Yoti may do additional checks against its\
            \ sources to confirm the user's identity."
          example: "+447777123456"
          nullable: true
          type: string
        structuredPostalAddress:
          allOf:
          - $ref: '#/components/schemas/YotiStructuredPostalAddress'
          description: |-
            A structured postal address for the individual that comes from the underlying document or can be manually added. In the case of being manually added, the address is considered "unverified".
            Unverified addresses may be safe to use. For example, Aadhaar card provides an address, but due to certain limitations of verifying Aadhaar, Yoti is unable to verify this property so it is listed as "unverified".
          nullable: true
        postalAddress:
          description: |-
            The whole address for the individual that comes from the underlying document or can be manually added. In the case of being manually added, the address is considered "unverified". The format of this will be different for each country.
            Refer to `StructuredPostalAddress` for a detailed version of the address.
          example: "6th Floor, 107 Leadenhall St, London, EC3A 4AF"
          nullable: true
          type: string
        documentDetails:
          allOf:
          - $ref: '#/components/schemas/YotiDocumentDetails'
          description: The details of the underlying document used to help create
            the Yoti credential
          nullable: true
      type: object
    ProblemDetails:
      additionalProperties: {}
      properties:
        type:
          description: A URI reference that identifies the problem type.
          nullable: true
          type: string
        title:
          description: "A short, human-readable summary of the error."
          nullable: true
          type: string
        status:
          description: The HTTP status code returned for the request.
          format: int32
          nullable: true
          type: integer
        detail:
          description: A human-readable explanation specific of the problem.
          nullable: true
          type: string
        instance:
          description: A URI reference that identifies the specific occurrence of
            the problem.
          nullable: true
          type: string
      type: object
    Provider:
      example:
        collectionMethod: PollResult
        subtext: dmv.ca.gov
        regions:
        - Europe
        licensed: true
        hasTrinsicInterface: true
        description: Identity verification via government ID and biometrics
        requiresInput: false
        countries:
        - US
        - CA
        availableAttributes:
        - scope: person.givenName
          outputted: Always
        - scope: person.givenName
          outputted: Always
        launchMethod: DeeplinkToMobile
        logoUrl: https://cdn.trinsic.id/providers/yoti.png
        liveHealth: Offline
        testHealth: Offline
        geography:
        - Global
        name: Yoti
        resultsMayBeDelayedAfterRedirect: false
        supportsDirectProviderSessions: true
        hasRefreshableContent: false
        id: yoti
        availableAttachments:
        - type: document_portrait
          outputted: Always
        - type: document_portrait
          outputted: Always
        subProviders:
        - subtext: dmv.ca.gov
          name: name
          id: id
          logoUrl: logoUrl
        - subtext: dmv.ca.gov
          name: name
          id: id
          logoUrl: logoUrl
        subdivisions:
        - US-CA
        - US-TX
      properties:
        id:
          description: The unique identifier of the provider
          example: yoti
          type: string
        name:
          description: The display name of the provider
          example: Yoti
          type: string
        logoUrl:
          description: The URL of the provider's logo
          example: https://cdn.trinsic.id/providers/yoti.png
          type: string
        subtext:
          description: "The Provider's subtext recommended to be shown next to the\
            \ name.\n            \nThis is flavor text, not a full, human-readable\
            \ description of the provider."
          example: dmv.ca.gov
          type: string
        description:
          deprecated: true
          description: A description of the provider's capabilities
          example: Identity verification via government ID and biometrics
          readOnly: true
          type: string
        geography:
          deprecated: true
          description: Geographic regions where this provider operates
          example:
          - Global
          items:
            type: string
          type: array
        regions:
          description: Specific regions supported by this provider
          example:
          - Europe
          items:
            type: string
          type: array
        countries:
          description: The countries where this Provider is available (as alpha-2
            ISO codes).
          example:
          - US
          - CA
          items:
            type: string
          type: array
        subdivisions:
          description: "The subdivisions (states, provinces, etc.) where this Provider\
            \ is available (as alpha-2 ISO codes)."
          example:
          - US-CA
          - US-TX
          items:
            type: string
          type: array
        licensed:
          description: Whether this provider is licensed for use by your organization
          example: true
          type: boolean
        launchMethod:
          allOf:
          - $ref: '#/components/schemas/IntegrationLaunchMethod'
          description: "Relevant only to Direct Provider Sessions.\n            \n\
            The `LaunchMethod` which must be supported to launch the Provider Session\
            \ in Direct Provider Sessions."
          example: DeeplinkToMobile
          x-enumDescriptions:
            LaunchBrowser: "The user's browser should be sent to the provided URL,\
              \ either in a new tab, pop-up, or top-level navigation."
            None: |-
              No action needs to be taken by the user; simply poll with the provided `resultsAccessKey`
              for the results of the session.
            DeeplinkToMobile: "The user must be sent to the provided URL, which is\
              \ a mobile deeplink.\n            \nThe URL should be shown as a button\
              \ on mobile, or as a QR code on desktop."
            ShowContent: Content must be displayed to the user to guide them through
              the process of verifying their identity.
            PerformNativeChallenge: "A challenge must be sent to a native API on the\
              \ user's device, such as the Digital Credentials API on web,\nor the\
              \ equivalent mDL exchange APIs on Android or iOS.\n            \nUse\
              \ Trinsic's UI SDKs on Web, Android, or iOS to streamline this process."
        collectionMethod:
          allOf:
          - $ref: '#/components/schemas/ResultCollectionMethod'
          description: "Relevant only to Direct Provider Sessions.\n            \n\
            The `CollectionMethod` which must be supported to launch the Provider\
            \ Session in Direct Provider Sessions."
          example: PollResult
          x-enumDescriptions:
            PollResult: "The results will be made available asynchronously, via a\
              \ polling mechanism.\n            \nUse the provided `ResultsAccessKey`\
              \ to poll for the results via the `GetResults` API."
            CaptureRedirect: "The user will be sent back to your application with\
              \ a `resultsAccessKey` in the query string.\n            \nUpon redirect,\
              \ use the provided `ResultsAccessKey` to retrieve the results via the\
              \ `GetResults` API."
            SubmitNativeChallengeResponse: "Submit a response to a native challenge\
              \ performed on the user's device, such as a native mDL exchange\nvia\
              \ the Digital Credentials API on web, or the equivalent native APIs\
              \ on Android or iOS.\n            \nSee the `PerformNativeChallenge`\
              \ launch method for more details."
        resultsMayBeDelayedAfterRedirect:
          description: "If `true`, then the results for this Provider may not be available\
            \ immediately after the user is redirected back\nto your application.\
            \ In this case, the `GetSessionResults` API must be called until results\
            \ are available.\n            \nThis is an uncommon scenario, and only\
            \ applies to Providers which cannot guarantee\nthe availability of results\
            \ immediately after the user is redirected back to your application."
          example: false
          type: boolean
        hasRefreshableContent:
          description: "Relevant only to Direct Provider Sessions.\n            \n\
            Whether the Provider requires the `RefreshStepContent` capability.\n \
            \           \nFor example, Samsung Wallet's deep links expire every 30\
            \ seconds, and must be refreshed periodically for a\nresilient user flow."
          example: false
          type: boolean
        requiresInput:
          description: "Relevant to Hosted Provider Sessions and Direct Provider Sessions.\n\
            \            \nIf `true`, this Provider requires provider-specific input\
            \ on Session creation.\nIf this input is not provided, Trinsic's Hosted\
            \ UI will be invoked to collect the input from the user."
          example: false
          type: boolean
        hasTrinsicInterface:
          description: "Whether there exists a Trinsic-hosted UI for this Provider.\n\
            \            \nThis is `true` for any Provider which is not a simple,\
            \ OIDC-like redirect flow."
          example: true
          type: boolean
        supportsDirectProviderSessions:
          description: "Whether this Provider can be fully whitelabeled/OEMed through\
            \ the Direct Provider Sessions API.\n            \nIf `false`, the Provider\
            \ may still be launched through Direct Provider Sessions;\nhowever, it\
            \ will necessarily require a Trinsic-hosted UI to function."
          example: true
          type: boolean
        availableAttributes:
          description: Information about the user attributes that this Provider will
            return in verification results.
          items:
            $ref: '#/components/schemas/ContractAttribute'
          nullable: true
          type: array
        availableAttachments:
          description: Information about the attachments that this Provider will return
            in verification results.
          items:
            $ref: '#/components/schemas/ContractAttachment'
          nullable: true
          type: array
        subProviders:
          description: "Metadata about the sub-providers which are available for this\
            \ Provider in the current Environment.\n            \nFor example, Italy's\
            \ SPID is a Provider which aggregates access to multiple sub-providers."
          items:
            $ref: '#/components/schemas/SubProviderMetadata'
          nullable: true
          type: array
        liveHealth:
          allOf:
          - $ref: '#/components/schemas/ProviderHealth'
          description: The health for a provider in the live environment
          example: Offline
          x-enumDescriptions:
            Online: The integration is up and available to support verification sessions.
            Offline: The integration has been disabled and unavailable at this time
              to perform verification sessions.
        testHealth:
          allOf:
          - $ref: '#/components/schemas/ProviderHealth'
          description: The health for a provider in the test environment
          example: Offline
          x-enumDescriptions:
            Online: The integration is up and available to support verification sessions.
            Offline: The integration has been disabled and unavailable at this time
              to perform verification sessions.
      required:
      - collectionMethod
      - countries
      - description
      - geography
      - hasRefreshableContent
      - hasTrinsicInterface
      - id
      - launchMethod
      - licensed
      - liveHealth
      - logoUrl
      - name
      - regions
      - requiresInput
      - resultsMayBeDelayedAfterRedirect
      - subdivisions
      - subtext
      - supportsDirectProviderSessions
      - testHealth
      type: object
    ProviderHealth:
      description: The health for an integration to be able to successfully perform
        a verification session.
      enum:
      - Online
      - Offline
      type: string
      x-enumDescriptions:
        Online: The integration is up and available to support verification sessions.
        Offline: The integration has been disabled and unavailable at this time to
          perform verification sessions.
    ProviderInput:
      properties:
        kenya-nid-match-biometric-2:
          allOf:
          - $ref: '#/components/schemas/KenyaNidBiometric2Input'
          description: Input for the `kenya-nid-match-biometric-2` provider
          nullable: true
        indonesia-nik-lookup:
          allOf:
          - $ref: '#/components/schemas/IndonesiaNikInput'
          description: Input for the `indonesia-nik-lookup` provider
          nullable: true
        indonesia-dukcapil-match:
          allOf:
          - $ref: '#/components/schemas/IndonesiaDukcapilMatchInput'
          description: Input for the `indonesia-dukcapil-match` provider
          nullable: true
        mexico-curp-lookup:
          allOf:
          - $ref: '#/components/schemas/MexicoCurpInput'
          description: Input for the `mexico-curp-lookup` provider
          nullable: true
        south-africa-nid-lookup:
          allOf:
          - $ref: '#/components/schemas/SouthAfricaNidInput'
          description: Input for the `south-africa-nid-lookup` provider
          nullable: true
        kenya-nid-lookup:
          allOf:
          - $ref: '#/components/schemas/KenyaNidInput'
          description: Input for the `kenya-nid-lookup` provider
          nullable: true
        kenya-nid-match-2:
          allOf:
          - $ref: '#/components/schemas/KenyaNidMatch2Input'
          description: Input for the `kenya-nid-match-2` provider
          nullable: true
        kenya-nid-lookup-2:
          allOf:
          - $ref: '#/components/schemas/KenyaNidLookup2Input'
          description: Input for the `kenya-nid-lookup-2` provider
          nullable: true
        south-africa-nid-lookup-2:
          allOf:
          - $ref: '#/components/schemas/SouthAfricaNidLookup2Input'
          description: Input for the `south-africa-nid-lookup-2` provider
          nullable: true
        south-africa-nid-match-2:
          allOf:
          - $ref: '#/components/schemas/SouthAfricaNidMatch2Input'
          description: Input for the `south-africa-nid-match-2` provider
          nullable: true
        south-africa-nid-match-biometric-2:
          allOf:
          - $ref: '#/components/schemas/SouthAfricaNidBiometric2Input'
          description: Input for the `south-africa-nid-match-biometric-2` provider
          nullable: true
        nigeria-nin-lookup-2:
          allOf:
          - $ref: '#/components/schemas/NigeriaNinLookup2Input'
          description: Input for the `nigeria-nin-lookup-2` provider
          nullable: true
        cote-divoire-nid-lookup-2:
          allOf:
          - $ref: '#/components/schemas/CoteDIvoireNidLookup2Input'
          description: Input for the `cote-divoire-nid-lookup-2` provider
          nullable: true
        zimbabwe-nid-lookup-2:
          allOf:
          - $ref: '#/components/schemas/ZimbabweNidLookup2Input'
          description: Input for the `zimbabwe-nid-lookup-2` provider
          nullable: true
        nigeria-nin-match-2:
          allOf:
          - $ref: '#/components/schemas/NigeriaNinMatch2Input'
          description: Input for the `nigeria-nin-match-2` provider
          nullable: true
        uganda-nid-match-2:
          allOf:
          - $ref: '#/components/schemas/UgandaNidMatch2Input'
          description: Input for the `uganda-nid-match-2` provider
          nullable: true
        nigeria-nin-match-biometric-2:
          allOf:
          - $ref: '#/components/schemas/NigeriaNinBiometric2Input'
          description: Input for the `nigeria-nin-match-biometric-2` provider
          nullable: true
        nigeria-nin-lookup:
          allOf:
          - $ref: '#/components/schemas/NigeriaNinInput'
          description: Input for the `nigeria-nin-lookup` provider
          nullable: true
        india-digilocker-aadhaar-match:
          allOf:
          - $ref: '#/components/schemas/AadhaarInput'
          description: Input for the `india-digilocker-aadhaar-match` provider
          nullable: true
        brazil-cpf-lookup:
          allOf:
          - $ref: '#/components/schemas/BrazilCpfCheckInput'
          description: Input for the `brazil-cpf-lookup` provider
          nullable: true
        brazil-digital-cnh:
          allOf:
          - $ref: '#/components/schemas/BrazilDigitalCnhInput'
          description: Input for the `brazil-digital-cnh` provider
          nullable: true
        philippines-philsys-match:
          allOf:
          - $ref: '#/components/schemas/PhilippineMatchInput'
          description: Input for the `philippines-philsys-match` provider
          nullable: true
        philippines-physical-national-id-qr:
          allOf:
          - $ref: '#/components/schemas/PhilippineQRInput'
          description: Input for the `philippines-physical-national-id-qr` provider
          nullable: true
        philippines-digital-national-id-qr:
          allOf:
          - $ref: '#/components/schemas/PhilippineQRInput'
          description: Input for the `philippines-digital-national-id-qr` provider
          nullable: true
        smart-id:
          allOf:
          - $ref: '#/components/schemas/SmartIdInput'
          description: Input for the `smart-id` provider
          nullable: true
        mobile-id:
          allOf:
          - $ref: '#/components/schemas/MobileIdInput'
          description: Input for the `mobile-id` provider
          nullable: true
        netherlands-idin:
          allOf:
          - $ref: '#/components/schemas/IdinInput'
          description: Input for the `netherlands-idin` provider
          nullable: true
        italy-spid:
          allOf:
          - $ref: '#/components/schemas/SpidInput'
          description: Input for the `italy-spid` provider
          nullable: true
        google-wallet:
          allOf:
          - $ref: '#/components/schemas/GoogleWalletInput'
          description: Input for the `google-wallet` provider
          nullable: true
        apple-wallet:
          allOf:
          - $ref: '#/components/schemas/AppleWalletInput'
          description: Input for the `apple-wallet` provider
          nullable: true
        peru-dni-lookup:
          allOf:
          - $ref: '#/components/schemas/PeruDniLookupInput'
          description: Input for the `peru-dni-lookup` provider
          nullable: true
        trinsic-test-database-lookup:
          allOf:
          - $ref: '#/components/schemas/TrinsicTestDatabaseLookupInput'
          description: "*TEST MODE ONLY.*\n            \nInput for the `trinsic-test-database-lookup`\
            \ provider"
          nullable: true
        trinsic-test-sub-providers:
          allOf:
          - $ref: '#/components/schemas/TrinsicTestSubProvidersInput'
          description: "*TEST MODE ONLY.*\n            \nInput for the `trinsic-test-sub-providers`\
            \ provider"
          nullable: true
      type: object
    ProviderOutput:
      properties:
        a-at-handy-signatur-login:
          allOf:
          - $ref: '#/components/schemas/AustriaHandySignaturProviderOutput'
          description: Exposed properties for the `a-at-handy-signatur-login` Provider
            which do not directly map to the normalized IdentityData model.
          nullable: true
        a-be-id-login:
          allOf:
          - $ref: '#/components/schemas/BelgiumIdCardProviderOutput'
          description: Exposed properties for the `a-be-id-login` Provider which do
            not directly map to the normalized IdentityData model.
          nullable: true
        a-edoapp-eid-login:
          allOf:
          - $ref: '#/components/schemas/PolandEdoAppProviderOutput'
          description: Exposed properties for the `a-edoapp-eid-login` Provider which
            do not directly map to the normalized IdentityData model.
          nullable: true
        a-ee-web-eid-login:
          allOf:
          - $ref: '#/components/schemas/EstoniaIdCardProviderOutput'
          description: Exposed properties for the `a-ee-web-eid-login` Provider which
            do not directly map to the normalized IdentityData model.
          nullable: true
        a-fi-id-login:
          allOf:
          - $ref: '#/components/schemas/FinlandIdCardProviderOutput'
          description: Exposed properties for the `a-fi-id-login` Provider which do
            not directly map to the normalized IdentityData model.
          nullable: true
        a-freja-eid-login:
          allOf:
          - $ref: '#/components/schemas/FrejaProviderOutput'
          description: Exposed properties for the `a-freja-eid-login` Provider which
            do not directly map to the normalized IdentityData model.
          nullable: true
        a-itsme-login:
          allOf:
          - $ref: '#/components/schemas/ItsmeProviderOutput'
          description: Exposed properties for the `a-itsme-login` Provider which do
            not directly map to the normalized IdentityData model.
          nullable: true
        a-lt-id-login:
          allOf:
          - $ref: '#/components/schemas/LithuaniaIdCardProviderOutput'
          description: Exposed properties for the `a-lt-id-login` Provider which do
            not directly map to the normalized IdentityData model.
          nullable: true
        a-lv-eparaksts-id-login:
          allOf:
          - $ref: '#/components/schemas/LatviaEparakstsProviderOutput'
          description: Exposed properties for the `a-lv-eparaksts-id-login` Provider
            which do not directly map to the normalized IdentityData model.
          nullable: true
        a-lv-eparaksts-mobile-login:
          allOf:
          - $ref: '#/components/schemas/LatviaEparakstsMobileProviderOutput'
          description: Exposed properties for the `a-lv-eparaksts-mobile-login` Provider
            which do not directly map to the normalized IdentityData model.
          nullable: true
        a-mojeid:
          allOf:
          - $ref: '#/components/schemas/CzechiaMojeIdProviderOutput'
          description: Exposed properties for the `a-mojeid` Provider which do not
            directly map to the normalized IdentityData model.
          nullable: true
        a-pl-mojeid-login:
          allOf:
          - $ref: '#/components/schemas/PolandMojeIdProviderOutput'
          description: Exposed properties for the `a-pl-mojeid-login` Provider which
            do not directly map to the normalized IdentityData model.
          nullable: true
        a-pt-id-login:
          allOf:
          - $ref: '#/components/schemas/PortugalIdCardProviderOutput'
          description: Exposed properties for the `a-pt-id-login` Provider which do
            not directly map to the normalized IdentityData model.
          nullable: true
        a-rs-id-login:
          allOf:
          - $ref: '#/components/schemas/SerbiaIdCardProviderOutput'
          description: Exposed properties for the `a-rs-id-login` Provider which do
            not directly map to the normalized IdentityData model.
          nullable: true
        apple-wallet:
          allOf:
          - $ref: '#/components/schemas/AppleWalletProviderOutput'
          description: Exposed properties for the `apple-wallet` Provider which do
            not directly map to the normalized IdentityData model.
          nullable: true
        denmark-mitid:
          allOf:
          - $ref: '#/components/schemas/MitIdProviderOutput'
          description: Exposed properties for the `denmark-mitid` Provider which do
            not directly map to the normalized IdentityData model.
          nullable: true
        ethiopia-fayda:
          allOf:
          - $ref: '#/components/schemas/FaydaProviderOutput'
          description: Exposed properties for the `ethiopia-fayda` Provider which
            do not directly map to the normalized IdentityData model.
          nullable: true
        france-identite-numerique:
          allOf:
          - $ref: '#/components/schemas/FrenchNumeriqueProviderOutput'
          description: Exposed properties for the `france-identite-numerique` Provider
            which do not directly map to the normalized IdentityData model.
          nullable: true
        google-wallet:
          allOf:
          - $ref: '#/components/schemas/GoogleWalletProviderOutput'
          description: Exposed properties for the `google-wallet` Provider which do
            not directly map to the normalized IdentityData model.
          nullable: true
        italy-spid:
          allOf:
          - $ref: '#/components/schemas/SpidProviderOutput'
          description: Exposed properties for the `italy-spid` Provider which do not
            directly map to the normalized IdentityData model.
          nullable: true
        kenya-nid-lookup-2:
          allOf:
          - $ref: '#/components/schemas/KenyaNidLookup2ProviderOutput'
          description: Exposed properties for the `kenya-nid-lookup-2` Provider which
            do not directly map to the normalized IdentityData model.
          nullable: true
        lloyds-smart-id:
          allOf:
          - $ref: '#/components/schemas/LloydsSmartIdProviderOutput'
          description: Exposed properties for the `lloyds-smart-id` Provider which
            do not directly map to the normalized IdentityData model.
          nullable: true
        mexico-curp-lookup:
          allOf:
          - $ref: '#/components/schemas/MexicoCurpProviderOutput'
          description: Exposed properties for the `mexico-curp-lookup` Provider which
            do not directly map to the normalized IdentityData model.
          nullable: true
        mobile-id:
          allOf:
          - $ref: '#/components/schemas/MobileIdProviderOutput'
          description: Exposed properties for the `mobile-id` Provider which do not
            directly map to the normalized IdentityData model.
          nullable: true
        peru-dni-lookup:
          allOf:
          - $ref: '#/components/schemas/PeruDniProviderOutput'
          description: Exposed properties for the `peru-dni-lookup` Provider which
            do not directly map to the normalized IdentityData model.
          nullable: true
        philippines-digital-national-id-qr:
          allOf:
          - $ref: '#/components/schemas/PhilippinesDigitalNidProviderOutput'
          description: Exposed properties for the `philippines-digital-national-id-qr`
            Provider which do not directly map to the normalized IdentityData model.
          nullable: true
        philippines-physical-national-id-qr:
          allOf:
          - $ref: '#/components/schemas/PhilippinesPhysicalNidProviderOutput'
          description: Exposed properties for the `philippines-physical-national-id-qr`
            Provider which do not directly map to the normalized IdentityData model.
          nullable: true
        post-office-easyid:
          allOf:
          - $ref: '#/components/schemas/PostOfficeEasyIdProviderOutput'
          description: Exposed properties for the `post-office-easyid` Provider which
            do not directly map to the normalized IdentityData model.
          nullable: true
        samsung-wallet:
          allOf:
          - $ref: '#/components/schemas/SamsungWalletProviderOutput'
          description: Exposed properties for the `samsung-wallet` Provider which
            do not directly map to the normalized IdentityData model.
          nullable: true
        smart-id:
          allOf:
          - $ref: '#/components/schemas/SmartIdProviderOutput'
          description: Exposed properties for the `smart-id` Provider which do not
            directly map to the normalized IdentityData model.
          nullable: true
        sweden-bankid:
          allOf:
          - $ref: '#/components/schemas/SwedenBankIdProviderOutput'
          description: Exposed properties for the `sweden-bankid` Provider which do
            not directly map to the normalized IdentityData model.
          nullable: true
        usa-louisiana-wallet:
          allOf:
          - $ref: '#/components/schemas/LaWalletProviderOutput'
          description: Exposed properties for the `usa-louisiana-wallet` Provider
            which do not directly map to the normalized IdentityData model.
          nullable: true
        yoti:
          allOf:
          - $ref: '#/components/schemas/YotiProviderOutput'
          description: Exposed properties for the `yoti` Provider which do not directly
            map to the normalized IdentityData model.
          nullable: true
      type: object
    RecommendProviderHealthOption:
      enum:
      - Online
      - Offline
      - All
      type: string
      x-enumDescriptions:
        Online: ""
        Offline: ""
        All: ""
    RecommendProviderInformation:
      example:
        subtext: dmv.ca.gov
        regions:
        - regions
        - regions
        name: name
        health: online | offline
        id: id
        countries:
        - countries
        - countries
        subProviders:
        - subtext: dmv.ca.gov
          name: name
          id: id
          logoUrl: logoUrl
        - subtext: dmv.ca.gov
          name: name
          id: id
          logoUrl: logoUrl
        logoUrl: logoUrl
        subdivisions:
        - subdivisions
        - subdivisions
      properties:
        id:
          description: The ID of the provider
          readOnly: true
          type: string
        name:
          description: "The friendly, human-readable name of the provider"
          readOnly: true
          type: string
        logoUrl:
          description: A URL pointing to the provider's logo
          readOnly: true
          type: string
        subtext:
          description: "The Provider's subtext recommended to be shown next to the\
            \ name.\n            \nThis is flavor text, not a full, human-readable\
            \ description of the provider."
          example: dmv.ca.gov
          readOnly: true
          type: string
        health:
          description: The current health status of the provider
          example: online | offline
          readOnly: true
          type: string
        subProviders:
          description: "Metadata about the sub-providers which are available for this\
            \ Provider.\n            \nFor example, Italy's SPID is a Provider which\
            \ aggregates access to multiple sub-providers."
          items:
            $ref: '#/components/schemas/SubProviderMetadata'
          nullable: true
          type: array
        regions:
          description: The regions a provider is available in.
          items:
            type: string
          readOnly: true
          type: array
        countries:
          description: "A list of countries, in alpha-2 ISO 3166-1 format, that the\
            \ provider is available in."
          items:
            type: string
          readOnly: true
          type: array
        subdivisions:
          description: "A list of subdivisions, in ISO 3166-2 format, that the provider\
            \ is available in."
          items:
            type: string
          readOnly: true
          type: array
      required:
      - countries
      - health
      - id
      - logoUrl
      - name
      - regions
      - subdivisions
      - subtext
      type: object
    RecommendProvidersRequest:
      example:
        verificationProfileId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        health: ""
        recommendationInfo: ""
      properties:
        verificationProfileId:
          description: The ID of the VerificationProfile to use for this recommendation.
          format: uuid
          type: string
        recommendationInfo:
          allOf:
          - $ref: '#/components/schemas/RecommendationInfo'
          description: Information about the user you wish to generate a recommendation
            for.
          nullable: true
        health:
          allOf:
          - $ref: '#/components/schemas/RecommendProviderHealthOption'
          description: Filter providers by health status.
          nullable: true
      required:
      - verificationProfileId
      type: object
    RecommendProvidersResponse:
      example:
        recommendedProviders:
        - subtext: dmv.ca.gov
          regions:
          - regions
          - regions
          name: name
          health: online | offline
          id: id
          countries:
          - countries
          - countries
          subProviders:
          - subtext: dmv.ca.gov
            name: name
            id: id
            logoUrl: logoUrl
          - subtext: dmv.ca.gov
            name: name
            id: id
            logoUrl: logoUrl
          logoUrl: logoUrl
          subdivisions:
          - subdivisions
          - subdivisions
        - subtext: dmv.ca.gov
          regions:
          - regions
          - regions
          name: name
          health: online | offline
          id: id
          countries:
          - countries
          - countries
          subProviders:
          - subtext: dmv.ca.gov
            name: name
            id: id
            logoUrl: logoUrl
          - subtext: dmv.ca.gov
            name: name
            id: id
            logoUrl: logoUrl
          logoUrl: logoUrl
          subdivisions:
          - subdivisions
          - subdivisions
      properties:
        recommendedProviders:
          items:
            $ref: '#/components/schemas/RecommendProviderInformation'
          type: array
      required:
      - recommendedProviders
      type: object
    RecommendationInfo:
      description: Information relating to a user; used to generated Identity Provider
        recommendations.
      properties:
        phoneNumber:
          description: "The phone number of the user you wish to generate a recommendation\
            \ for.\n            \nWill be used to look up the user's identity in the\
            \ network, as well as to determine the user's geographic location."
          nullable: true
          type: string
        countries:
          description: "A list of countries, in alpha-2 ISO 3166 format, which the\
            \ user is related to.\n            \nThis can include the user's country\
            \ of residence, nationality, etc."
          items:
            type: string
          nullable: true
          type: array
        subdivisions:
          description: "If one of the countries has subdivisions (for example: US\
            \ states), specify those related to the user here (e.g., CA, UT, NY)"
          items:
            type: string
          nullable: true
          type: array
        ipAddresses:
          description: "Any IP addresses related to the user.\n            \nWill\
            \ be used to determine the user's geographic location."
          items:
            type: string
          nullable: true
          type: array
      type: object
    RedirectUriResponse:
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        uri: uri
      properties:
        id:
          format: uuid
          type: string
        uri:
          minLength: 1
          type: string
      required:
      - id
      - uri
      type: object
    RefreshStepContentRequest:
      example:
        resultsAccessKey: resultsAccessKey
      properties:
        resultsAccessKey:
          description: The `ResultsAccessKey` returned when the Session was created
          minLength: 1
          type: string
      required:
      - resultsAccessKey
      type: object
    RefreshStepContentResponse:
      example:
        nextStep: ""
      properties:
        nextStep:
          allOf:
          - $ref: '#/components/schemas/IntegrationStep'
          description: The integration's next step with refreshed content
      required:
      - nextStep
      type: object
    ResultCollection:
      properties:
        method:
          allOf:
          - $ref: '#/components/schemas/ResultCollectionMethod'
          description: The method by which the results of the Acceptance Session should
            be collected.
          example: CaptureRedirect
          x-enumDescriptions:
            PollResult: "The results will be made available asynchronously, via a\
              \ polling mechanism.\n            \nUse the provided `ResultsAccessKey`\
              \ to poll for the results via the `GetResults` API."
            CaptureRedirect: "The user will be sent back to your application with\
              \ a `resultsAccessKey` in the query string.\n            \nUpon redirect,\
              \ use the provided `ResultsAccessKey` to retrieve the results via the\
              \ `GetResults` API."
            SubmitNativeChallengeResponse: "Submit a response to a native challenge\
              \ performed on the user's device, such as a native mDL exchange\nvia\
              \ the Digital Credentials API on web, or the equivalent native APIs\
              \ on Android or iOS.\n            \nSee the `PerformNativeChallenge`\
              \ launch method for more details."
        resultsAccessKey:
          description: "The `resultsAccessKey` for the Acceptance Session.\n     \
            \       \nThis is an encrypted payload which contains the decryption key\
            \ necessary to access the Session's Data Vault.\n            \nSave this\
            \ securely in your systems; it must be passed back with any API call which\
            \ requires access to the Session's Data Vault.\n            \nTrinsic\
            \ cannot access a Session's Data Vault without this key."
          example: 417zNGHMJp6Ht2jCoktTLNGDRZEt4BqTwwQ8Q9ZaQz8aS4Jd7nD1xff2WLhPS69j5q9QT4gvgV79Gv7QtaXwu5YC9Utr35...
          type: string
      required:
      - method
      - resultsAccessKey
      type: object
    ResultCollectionMethod:
      enum:
      - PollResult
      - CaptureRedirect
      - SubmitNativeChallengeResponse
      type: string
      x-enumDescriptions:
        PollResult: "The results will be made available asynchronously, via a polling\
          \ mechanism.\n            \nUse the provided `ResultsAccessKey` to poll\
          \ for the results via the `GetResults` API."
        CaptureRedirect: "The user will be sent back to your application with a `resultsAccessKey`\
          \ in the query string.\n            \nUpon redirect, use the provided `ResultsAccessKey`\
          \ to retrieve the results via the `GetResults` API."
        SubmitNativeChallengeResponse: "Submit a response to a native challenge performed\
          \ on the user's device, such as a native mDL exchange\nvia the Digital Credentials\
          \ API on web, or the equivalent native APIs on Android or iOS.\n       \
          \     \nSee the `PerformNativeChallenge` launch method for more details."
    SamsungWalletProviderOutput:
      description: Exposed properties for the `samsung-wallet` Provider which do not
        directly map to the normalized IdentityData model.
      properties:
        rawMdlOutput:
          allOf:
          - $ref: '#/components/schemas/MdlOutput'
          description: The raw output of the mDL exchange performed through Samsung
            Wallet.
      required:
      - rawMdlOutput
      type: object
    SerbiaIdCardProviderOutput:
      description: Exposed properties for the `a-rs-id-login` Provider which do not
        directly map to the normalized IdentityData model.
      properties:
        firstName:
          description: The first name of the verified individual
          example: John
          type: string
        lastName:
          description: The last name of the verified individual
          example: Doe
          type: string
        dateOfBirth:
          description: The date of birth of the verified individual
          example: 1985-12-10
          format: date
          type: string
        uniqueMasterCitizenNumber:
          description: "The 13-digit Serbian Unique Master Citizen Number (\"JMBG\"\
            \ / \"Jedinstveni Matični Broj Građana\") of the verified individual.\n\
            \            \nThis is in the format DDMMYYYRRSSSC, where:\n- DDMM is\
            \ the day and month of birth\n- YYY is the last three digits of the year\
            \ of birth\n- RR is the political region code of the region of birth (if\
            \ born after 1976) or of first registration (if born before 1976)\n- SSS\
            \ is a unique sex-specific serial number for individuals born on the same\
            \ date in the same region\n- C is a checksum digit\n            \nIf YYY\
            \ is between 000 and 099, the millennium digit of the year is \"2\"; the\
            \ individual was born after the year 2000.\nIf YYY is between 800 and\
            \ 999, the millennium digit of the year is \"1\"; the individual was born\
            \ before the year 2000.\n            \nIf SSS is between 000 and 499,\
            \ the individual is male.\nIf SSS is between 500 and 999, the individual\
            \ is female."
          example: "3006990711231"
          type: string
      required:
      - dateOfBirth
      - firstName
      - lastName
      - uniqueMasterCitizenNumber
      type: object
    Session:
      example:
        success: true
        created: 1722526411
        errorCode: ""
        id: 678c1ff6-2178-49e7-81d2-548afb6f85ec
        done: true
        updated: 1722526722
      properties:
        id:
          example: 678c1ff6-2178-49e7-81d2-548afb6f85ec
          type: string
        done:
          description: "Whether the Session is in a terminal / final state.\n    \
            \        \nIf this is `true`, inspect the value of `Success` to determine\
            \ whether the Session was successful."
          type: boolean
        success:
          description: "Whether the Session has completed successfully.\n        \
            \    \nIf this is `false`, the Session is either not yet done, or has\
            \ failed. Inspect `Done` and `ErrorCode` for more information.\nIf this\
            \ is `true`, the Session has completed successfully."
          type: boolean
        errorCode:
          allOf:
          - $ref: '#/components/schemas/SessionErrorCode'
          description: "The reason for the Session's failure.\n            \nOnly\
            \ present if `Success` is `false`."
          nullable: true
        created:
          description: "The unix timestamp, in seconds, when this session was created"
          example: 1722526411
          format: int64
          type: integer
        updated:
          description: "The unix timestamp, in seconds, when this session's data was\
            \ last updated"
          example: 1722526722
          format: int64
          type: integer
      required:
      - created
      - done
      - id
      - success
      - updated
      type: object
    SessionErrorCode:
      enum:
      - Internal
      - Other
      - Canceled
      - UserAbandoned
      - UserTimedOut
      - Expired
      - InvalidImage
      - Inauthentic
      - UnsupportedDocument
      - AssuranceLevelNotMet
      - SecurityValidationFailed
      - InvalidResponse
      type: string
      x-enumDescriptions:
        Internal: An internal error has caused this session to fail
        Other: The session failed for a reason which does not fit any other failure
          code.
        Canceled: The session has been canceled
        UserAbandoned: The user abandoned the session
        UserTimedOut: The user took too long to complete the verification
        Expired: The session has expired
        InvalidImage: |-
          The image(s) provided for this verification were either too low-quality, not of the correct type, or otherwise
          unable to be processed.
        Inauthentic: "The identity data/images provided are suspected to be inauthentic,\
          \ fraudulent, or forged."
        UnsupportedDocument: "The document provided is either of an unsupported type,\
          \ or from an unsupported country."
        AssuranceLevelNotMet: The user's identity does not meet the required assurance
          level for this verification.
        SecurityValidationFailed: A critical security validation (such as a cryptographic
          signature check or secure handshake with the provider) has failed.
        InvalidResponse: "The response from the identity provider was malformed, incomplete,\
          \ or otherwise could not be processed."
    SessionOrdering:
      enum:
      - Created
      - Updated
      - State
      type: string
      x-enumDescriptions:
        Created: Order sessions by their creation date
        Updated: Order sessions by the date their state last changed
        State: Order sessions by their state
    Sex:
      enum:
      - Unknown
      - NotApplicable
      - Male
      - Female
      type: string
      x-enumDescriptions:
        Unknown: ""
        NotApplicable: ""
        Male: ""
        Female: ""
    SmartIdInput:
      properties:
        smartIdNumber:
          description: The user's Smart ID ETSI number or document number.
          nullable: true
          type: string
      type: object
    SmartIdProviderOutput:
      description: Exposed properties for the `smart-id` Provider which do not directly
        map to the normalized IdentityData model.
      properties:
        givenName:
          description: |-
            The given name (first name) of the individual, extracted from the Smart ID
            authentication certificate's Subject Distinguished Name "G" (givenName) attribute.
          example: MARI
          nullable: true
          type: string
        familyName:
          description: |-
            The family name (surname) of the individual, extracted from the Smart ID
            authentication certificate's Subject Distinguished Name "SN" (surname) attribute.
          example: TAMM
          nullable: true
          type: string
        dateOfBirth:
          description: "The individual's date of birth, derived from the personal\
            \ code.\n            \nNOTE: Newer Latvian personal codes starting with\
            \ \"32\" do not contain\ndate of birth information."
          example: 1985-01-01
          format: date
          nullable: true
          type: string
        sex:
          description: "The individual's sex, derived from the first digit of the\
            \ personal code.\n            \nNOTE: Not available for Latvian personal\
            \ codes.\n            \nPossible values:\n- Male\n- Female"
          example: Female
          nullable: true
          type: string
        country:
          description: "The ISO 3166-1 alpha-2 country code extracted from the Smart\
            \ ID authentication\ncertificate's Subject Distinguished Name \"C\" (country)\
            \ attribute.\n            \nThis represents the country of the certificate\
            \ issuer, not the person's nationality\nor citizenship. For Smart ID,\
            \ this will be \"EE\" (Estonia), \"LT\" (Lithuania), or\n\"LV\" (Latvia)."
          example: EE
          nullable: true
          type: string
        identityType:
          description: "The identity document type, extracted from the first 3 characters\
            \ of the\nSERIALNUMBER field (before the country code).\n            \n\
            Possible values:\n- PNO: Personal Number (national civic registration\
            \ number)\n- PAS: Passport\n- IDC: National identity card"
          example: PNO
          nullable: true
          type: string
        personalCode:
          description: "The personal code (Estonian: isikukood, Lithuanian: asmens\
            \ kodas, Latvian: personas kods)\nof the individual, extracted from the\
            \ SERIALNUMBER field of the Smart ID authentication\ncertificate. This\
            \ is the portion after the identity type and country prefix\n(e.g., \"\
            48501010014\" from \"PNOEE-48501010014\").\n            \nEstonian and\
            \ Lithuanian personal codes are 11 digits in the format GYYMMDDSSSC where:\n\
            - G = century/gender (3-4 = 1900s, 5-6 = 2000s; odd = male, even = female)\n\
            - YYMMDD = date of birth\n- SSS = sequence number\n- C = checksum digit\n\
            \            \nLatvian personal codes use the format DDMMYY-NNNNN (6 digits,\
            \ a dash, and 5 digits).\n            \nSee:\n- https://en.wikipedia.org/wiki/National_identification_number#Estonia\n\
            - https://en.wikipedia.org/wiki/National_identification_number#Lithuania\n\
            - https://en.wikipedia.org/wiki/National_identification_number#Latvia"
          example: "48501010014"
          nullable: true
          type: string
        serialNumber:
          description: "The SERIALNUMBER attribute from the Smart ID authentication\
            \ certificate's Subject\nDistinguished Name. Format: \"{identity-type}{country-code}-{identifier}\"\
            \n            \nComponents:\n- identity-type (3 chars): PNO (Personal\
            \ Number), PAS (Passport), IDC (ID Card)\n- country-code (2 chars): ISO\
            \ 3166-1 alpha-2 (EE, LT, LV)\n- identifier: The personal code"
          example: PNOEE-48501010014
          nullable: true
          type: string
        certificateSubject:
          description: |-
            The full Subject Distinguished Name (Subject DN) from the Smart ID
            authentication certificate. Contains comma-separated RDN (Relative Distinguished Name)
            components including C (Country), CN (Common Name), SN (Surname), G (Given Name),
            and SERIALNUMBER (Personal identifier).
          example: "SERIALNUMBER=PNOEE-48501010014, G=MARI, SN=TAMM, CN=\"TAMM,MARI,48501010014\"\
            , C=EE"
          nullable: true
          type: string
        certificateLevel:
          description: "The certificate level indicating the authentication assurance\
            \ level.\n            \nPossible values:\n- QUALIFIED: Highest assurance\
            \ level (eIDAS QES - Qualified Electronic Signature),\n  legally equivalent\
            \ to a handwritten signature. This is the standard production level.\n\
            - ADVANCED: Lower assurance level (eIDAS AdES), also called \"Smart-ID\
            \ Basic\".\n  Only available in test environments for test accounts."
          example: QUALIFIED
          nullable: true
          type: string
        documentNumber:
          description: "A unique identifier for the Smart ID account/document used\
            \ for this authentication.\nFormat: PNO{CC}-{personal-code}-{device-id}-{qualification}\n\
            \            \nComponents:\n- PNO = Personal Number (identity type)\n\
            - CC = Country code (EE, LT, LV)\n- personal-code = The individual's personal\
            \ code\n- device-id = Random 4-character device identifier\n- qualification\
            \ = NQ (non-qualified) or Q (qualified certificate)\n            \nThis\
            \ is a stable identifier that can be used to recognize returning users\
            \ across sessions."
          example: PNOEE-48501010014-Z1B2-Q
          nullable: true
          type: string
        interactionFlowUsed:
          description: "Indicates which interaction flow was used during the Smart\
            \ ID authentication process.\nThis reflects which of the allowedInteractionsOrder\
            \ options was actually used.\n            \nPossible values:\n- displayTextAndPIN:\
            \ User saw text and entered PIN\n- verificationCodeChoice: User selected\
            \ from multiple verification codes\n- confirmationMessage: User confirmed\
            \ a message\n- confirmationMessageAndVerificationCodeChoice: Combination\
            \ of both"
          example: displayTextAndPIN
          nullable: true
          type: string
        deviceIpAddress:
          description: |-
            The IP address of the device where the Smart ID app was used for authentication.
            Can be IPv4 or IPv6 format.
          example: 192.168.1.100
          nullable: true
          type: string
      type: object
    SouthAfricaNidBiometric2Input:
      properties:
        idNumber:
          description: The user's South African National ID number
          nullable: true
          type: string
        livenessImages:
          description: |-
            An array of exactly 8 images required for biometric liveness verification.
            The first 7 images should be liveness frames captured during the liveness detection process,
            and the last image (8th) should be a selfie of the user. All images must be in JPEG format
            and each image must be less than 15MB in size.
          items:
            format: byte
            type: string
          nullable: true
          type: array
        testSelfie:
          description: "Test selfie for test environment (optional, maximum 15MB).\
            \ Must be JPEG format."
          format: byte
          nullable: true
          type: string
      type: object
    SouthAfricaNidInput:
      properties:
        fullName:
          description: The user's full name as it appears in their National ID
          minLength: 1
          type: string
        dateOfBirth:
          description: "The user's date of birth, in `YYYY-MM-DD` format"
          format: date
          type: string
        gender:
          description: "The user's gender as it appears in their National ID (e.g.,\
            \ male, female)"
          minLength: 1
          type: string
        nationalIdNumber:
          description: The user's National ID number
          maxLength: 13
          minLength: 13
          pattern: ^\d*$
          type: string
      required:
      - dateOfBirth
      - fullName
      - gender
      - nationalIdNumber
      type: object
    SouthAfricaNidLookup2Input:
      properties:
        idNumber:
          description: The user's National ID number
          nullable: true
          type: string
      type: object
    SouthAfricaNidMatch2Input:
      properties:
        idNumber:
          description: The user's South African National ID number
          nullable: true
          type: string
        givenName:
          description: The user's first name as it appears in their National ID
          nullable: true
          type: string
        familyName:
          description: The user's last name as it appears in their National ID
          nullable: true
          type: string
        middleName:
          description: The user's middle name as it appears in their National ID (optional)
          nullable: true
          type: string
        dateOfBirth:
          description: "The user's date of birth, in `YYYY-MM-DD` format"
          format: date
          nullable: true
          type: string
        sex:
          allOf:
          - $ref: '#/components/schemas/SouthAfricaNidMatch2InputSex'
          description: The user's sex as it appears in their National ID
          nullable: true
        phoneNumber:
          description: |-
            The user's phone number as it appears in their National ID (optional).
            Must be in E.164 international format: "+27XXXXXXXXX"
          nullable: true
          type: string
      type: object
    SouthAfricaNidMatch2InputSex:
      enum:
      - Male
      - Female
      type: string
      x-enumDescriptions:
        Male: ""
        Female: ""
    SpidBillingInformation:
      description: Billing information for a SPID Verification.
      properties:
        isBillable:
          description: Whether this SPID verification has resulted in a billable event.
          example: true
          type: boolean
        verificationType:
          description: "The billable verification type for this SPID verification.\n\
            \            \nPossible values: \"Authentication\" | \"Registration\""
          example: '"Authentication"'
          type: string
        verificationLevel:
          description: "The billable verification level for this SPID verification.\n\
            \            \nPossible values: 1 | 2 | 3"
          example: 1
          format: int32
          type: integer
      required:
      - isBillable
      - verificationLevel
      - verificationType
      type: object
    SpidInput:
      properties:
        subProviderId:
          description: "The ID of the specific IDP to invoke within SPID.\n      \
            \      \nIf not specified, the user will be prompted to select an IDP."
          nullable: true
          type: string
        billingTrackingSecret:
          description: "Only applicable if period-based billing is enabled for your\
            \ Verification Profile. Contact Trinsic to enable this.\n            \n\
            A secret UTF-8 string between 32 and 64 characters in length, used to\
            \ enable privacy-preserving tracking of unique user verifications during\
            \ a billing period.\n            \nWARNING: This value must NOT change\
            \ during the course of a billing period for a given Verification Profile,\
            \ or double-billing may occur.\nIf multiple Verification Profiles are\
            \ configured to use the same Trinsic-managed SPID Service Provider, the\
            \ same Billing Tracking Secret must be provided across all such Verification\
            \ Profiles."
          nullable: true
          type: string
      type: object
    SpidProviderOutput:
      description: Exposed properties for the `italy-spid` Provider which do not directly
        map to the normalized IdentityData model.
      properties:
        billingInformation:
          allOf:
          - $ref: '#/components/schemas/SpidBillingInformation'
          description: "Information about the billable status of this SPID Verification.\n\
            \            \nPresent only if your account has period-based billing enabled\
            \ for SPID. Contact Trinsic to enable this."
          nullable: true
        identityProviderEntityId:
          description: "The SPID Entity ID of the Identity Provider which issued the\
            \ SPID identity.\n            \nThis is an HTTPS URI which uniquely identifies\
            \ the IdP within the SPID federation.\n            \nA normalized / simplified\
            \ representation of this value is present in the `originatingSubProviderId`\
            \ field in Trinsic's normalized data model."
          example: https://spid.register.it
          type: string
        spidCode:
          description: "The identifier of the SPID credential, uniquely identifying\
            \ it within the SPID federation.\n            \nThe format of this string\
            \ is specific to each individual Identity Provider."
          example: SPID-002
          type: string
        spidCredentialExpirationDate:
          description: "Expiration date of the SPID credential.\n            \nThis\
            \ is not the same as the expiration date of the underlying identity document\
            \ (such as a passport) which was\nused to create the SPID identity."
          example: 2030-12-21
          format: date
          nullable: true
          type: string
        rawIdCard:
          description: "The raw, space-separated string value for the \"IdCard\" field\
            \ from the SPID credential.\n            \nTrinsic additionally parses\
            \ this field and uses it to populate the `Document` object in the normalized\
            \ data model."
          example: passaporto KK1234567 questuraLivorno 2016-09-04 2026-09-03
          nullable: true
          type: string
        givenName:
          description: The individual's first / given name(s)
          example: Ada
          nullable: true
          type: string
        familyName:
          description: The individual's last / family name(s)
          example: Lovelace
          nullable: true
          type: string
        dateOfBirth:
          description: The date of birth of the individual
          example: 1985-12-10
          format: date
          nullable: true
          type: string
        gender:
          description: "The gender of the individual.\n            \nPossible values:\
            \ \"M\" | \"F\""
          example: F
          nullable: true
          type: string
        placeOfBirth:
          description: "The individual's place of birth.\n            \nThis is a\
            \ 4-digit Belfiore Code identifying either the municipality of birth (for\
            \ births in Italy) or a foreign country (for births outside Italy)."
          example: G702
          nullable: true
          type: string
        countyOfBirth:
          description: "The individual's county (province) of birth, abbreviated.\n\
            \            \nFor foreign births, this is \"EE\"."
          example: PI
          nullable: true
          type: string
        domicileStreetAddress:
          description: "The street address of the individual's residence.\n      \
            \      \nThis includes street name, street type, and house number, in\
            \ the standard format for the given state."
          example: Via Liszt 21
          nullable: true
          type: string
        domicilePostalCode:
          description: The postal code of the individual's residence
          example: "00144"
          nullable: true
          type: string
        domicileMunicipality:
          description: The municipality of the individual's residence
          example: Rome
          nullable: true
          type: string
        domicileProvince:
          description: The province of the individual's residence
          example: RM
          nullable: true
          type: string
        domicileNation:
          description: The 2-digit country code of the individual's residence
          example: IT
          nullable: true
          type: string
        email:
          description: The email address of the individual
          example: example@example.com
          nullable: true
          type: string
        mobilePhone:
          description: "The mobile phone number of the individual, in Italian format."
          example: "34912345678"
          nullable: true
          type: string
        digitalAddress:
          description: The PEC (Italian Certified Email) address of the individual
            or organization.
          example: name@pecdomain.it
          nullable: true
          type: string
        fiscalNumber:
          description: Fiscal tax number for the individual.
          example: TINIT-ABCXYZ00W00Z000Z
          nullable: true
          type: string
        ivaCode:
          description: "The VAT number of the organization which was verified, or\
            \ of the organization which the verified individual is associated with."
          example: VATIT-12345678901
          nullable: true
          type: string
        companyName:
          description: "The name of the organization which was verified, or of the\
            \ organization which the verified individual is associated with."
          example: ACME Corporation
          nullable: true
          type: string
        companyFiscalNumber:
          description: "The fiscal tax number of the organization which was verified,\
            \ or of the organization which the verified individual is associated with."
          example: TINIT-12345678901
          nullable: true
          type: string
        registeredOffice:
          description: "The registered office address of the organization which was\
            \ verified, or of the organization which the verified individual is associated\
            \ with."
          example: via Listz 21 00144 Rome
          nullable: true
          type: string
      required:
      - identityProviderEntityId
      - spidCode
      type: object
    StepRefreshInfo:
      properties:
        expiresAt:
          description: "The UTC date/time at which the step `content` will expire\
            \ and should no longer be used.\n            \nUse the `Refresh Step Content`\
            \ API to obtain a new value for `content`."
          format: date-time
          type: string
        refreshAfter:
          description: The UTC date/time after which Trinsic recommends you refresh
            the step `content`.
          format: date-time
          type: string
        timeToLiveSeconds:
          description: The total lifetime of the step `content`.
          format: int32
          type: integer
      required:
      - expiresAt
      - refreshAfter
      - timeToLiveSeconds
      type: object
    SubProviderMetadata:
      description: Information about a sub-provider.
      example:
        subtext: dmv.ca.gov
        name: name
        id: id
        logoUrl: logoUrl
      properties:
        id:
          description: "The ID of the sub-provider.\n            \nThis cannot be\
            \ used as a standalone Provider ID when creating a Session. It must\n\
            be passed in via the Provider-specific input."
          type: string
        name:
          description: The name of the sub-provider
          type: string
        subtext:
          description: "The Provider's subtext recommended to be shown next to the\
            \ name.\n            \nThis is flavor text, not a full, human-readable\
            \ description of the provider."
          example: dmv.ca.gov
          type: string
        logoUrl:
          description: "A URL pointing to the logo on Trinsic's CDN.\n           \
            \ \nMay be a PNG, JPG, or SVG image."
          type: string
      required:
      - id
      - logoUrl
      - name
      - subtext
      type: object
    SubmitNativeChallengeResponseRequest:
      example:
        responseToken: responseToken
        resultsAccessKey: resultsAccessKey
      properties:
        resultsAccessKey:
          description: The Results Access Key for the Session in question
          minLength: 1
          type: string
        responseToken:
          description: The response token retrieved from a Trinsic UI SDK's performMdlExchange()
            call.
          minLength: 1
          type: string
      required:
      - responseToken
      - resultsAccessKey
      type: object
    SubmitNativeChallengeResponseResponse:
      example:
        session: ""
      properties:
        session:
          allOf:
          - $ref: '#/components/schemas/Session'
          description: The Session for which the challenge token was submitted
      required:
      - session
      type: object
    SwedenBankIdProviderOutput:
      description: Exposed properties for the `sweden-bankid` Provider which do not
        directly map to the normalized IdentityData model.
      properties:
        fullName:
          description: The full name of the individual.
          example: Ove Henriksen
          nullable: true
          type: string
        dateOfBirth:
          description: The date of birth of the individual.
          example: 1943-08-15
          format: date
          nullable: true
          type: string
        personalIdentityNumber:
          description: "The Swedish personal number (personnummer) associated with\
            \ the verified individual. The value is typically a 12-digit number.\n\
            This is in the format YYYYMMDDXXXX, where:\n- YYYYMMDD is the date of\
            \ birth.\n- XXXX is an individual number, indicating gender.\n       \
            \     \nIf XXXX is even, the individual is female.\nIf XXXX is odd, the\
            \ individual is male."
          example: 20200101-1231
          nullable: true
          type: string
        givenName:
          description: The given name of the individual
          example: Ove
          nullable: true
          type: string
        familyName:
          description: The family name of the individual
          example: Henriksen
          nullable: true
          type: string
        country:
          description: The end user's country on the verified certificate. This is
            ISO 2-character code of the country.
          example: SE
          nullable: true
          type: string
        certificatePolicy:
          description: The object identifier policy of the individual's verified certificate
          example: 1.2.752.78.1.1
          nullable: true
          type: string
        commonName:
          description: The common name of the end user's verified certificate
          example: Ove Henriksenn
          nullable: true
          type: string
        distinguishedName:
          description: The distinguished name of the end user's verified certificate
          example: "N=Ove Henriksen, OID.2.5.4.41=(180427 13.09) Ove Henriksen - BankID\
            \ på fil, SERIALNUMBER=19531​​0021935, GIVENNAME=Ove, SURNAME=Henriksen,\
            \ O=Testbank A AB (publ), C=SE ​"
          nullable: true
          type: string
        personalIdentityNumberIssuingCountry:
          description: The country that issued the Swedish personal number. This is
            an ISO 2-character code of the country and is extracted from the certificate.
            This will always be `SE`.
          example: SE
          nullable: true
          type: string
        authenticationDeviceIp:
          description: The IP address of the device used for authentication
          example: 192.168.1.100
          nullable: true
          type: string
        notBefore:
          description: The certificate validity start date (not before) in UTC
          example: 2024-01-01T00:00:00Z
          format: date-time
          nullable: true
          type: string
        notAfter:
          description: The certificate validity end date (not after) in UTC
          example: 2029-06-30T23:59:59Z
          format: date-time
          nullable: true
          type: string
      type: object
    TrinsicTestDatabaseLookupInput:
      properties:
        givenName:
          description: "The given name to use for the output of the test Session.\n\
            \            \nThis is required; if not provided, Trinsic's Fallback UI\
            \ will be invoked to collect it\nfrom the user.\n            \nCan be\
            \ any non-empty value."
          nullable: true
          type: string
        familyName:
          description: "The family name to use for the output of the test Session.\n\
            \            \nThis is required; if not provided, Trinsic's Fallback UI\
            \ will be invoked to collect it\nfrom the user.\n            \nCan be\
            \ any non-empty value."
          nullable: true
          type: string
        identityCode:
          description: "A 6-digit code; must be \"123456\" for the Session to succeed.\n\
            \            \nThis is required; if not provided, Trinsic's Fallback UI\
            \ will be invoked to collect it\nfrom the user.\n            \nAny other\
            \ value will cause the Session to fail."
          nullable: true
          type: string
        selfieBase64:
          description: "An optional selfie image, base64-encoded.\n            \n\
            Will replace the existing test selfie attachment output if provided."
          nullable: true
          type: string
      type: object
    TrinsicTestSubProvidersInput:
      properties:
        subProviderId:
          description: "The ID of the specific IDP to invoke within the test federated\
            \ provider.\n            \nValid options are `sub-provider-a` and `sub-provider-b`.\n\
            \            \nIf not specified, the user will be prompted to select one."
          nullable: true
          type: string
      type: object
    UgandaNidMatch2Input:
      properties:
        idNumber:
          description: The user's Uganda National ID number (NIN) - 14 alphanumeric
            characters
          nullable: true
          type: string
        secondaryIdNumber:
          description: |-
            The card number on the document (secondary ID number).
            Required for Uganda Basic KYC instead of first name and last name.
          nullable: true
          type: string
        dateOfBirth:
          description: "The user's date of birth, in `YYYY-MM-DD` format"
          format: date
          nullable: true
          type: string
      type: object
    VerificationProfileResponse:
      example:
        enabledProviders:
        - enabledProviders
        - enabledProviders
        brandName: brandName
        primaryColor: primaryColor
        alias: alias
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        isProductionUsage: true
        logoUrl: logoUrl
      properties:
        id:
          description: The ID of the verification profile
          format: uuid
          type: string
        alias:
          description: An alias of the verification profile shown to developers and
            administrators.
          type: string
        brandName:
          description: The brand name of the verification profile shown to end-users.
          type: string
        logoUrl:
          description: The URL of the verification profile's logo.
          type: string
        primaryColor:
          description: The primary color of the verification profile.
          type: string
        enabledProviders:
          description: The providers that are currently enabled for the verification
            profile.
          items:
            type: string
          type: array
        isProductionUsage:
          description: Whether this profile is for production usage. Only applicable
            for Live environment profiles.
          type: boolean
      required:
      - alias
      - brandName
      - enabledProviders
      - id
      - isProductionUsage
      - logoUrl
      - primaryColor
      type: object
    YotiDocumentDetails:
      properties:
        type:
          description: "The type of document used to create the Yoti credential.\n\
            \            \nPossible values:\n- PASSPORT: A government-issued identity\
            \ document\n- DRIVING_LICENCE: A government-issued identity card\n- NATIONAL_ID:\
            \ A government-issued identity card\n- PASS_CARD: A physical or digital\
            \ identity card that is accredited by the UK's national Proof of Age Standards\
            \ Scheme (PASS)"
          example: PASSPORT
          nullable: true
          type: string
        issuingCountry:
          description: The issuing country's ISO alpha-3 code
          example: GBR
          nullable: true
          type: string
        documentNumber:
          description: The arbitrary document number for the provided document type
          example: "123456789"
          nullable: true
          type: string
        expirationDate:
          description: The document's expiration date in YYYY-MM-DD format
          example: 2016-03-21
          format: date
          nullable: true
          type: string
        issuingAuthority:
          description: The document's issuing authority. This can either be a country
            code as a specified ISO alpha-3 or the name of the issuing authority.
          example: GBR
          nullable: true
          type: string
      type: object
    YotiProviderOutput:
      description: Exposed properties for the `yoti` Provider which do not directly
        map to the normalized IdentityData model.
      properties:
        rememberMeId:
          description: "The unique id for your service of the individual. This is\
            \ not shared between different services so if the user logs into a different\
            \ service, the user will have a new id for the other service."
          example: 9f63ff98-e1e3-433c-9b86-7c697d0b4e5f
          format: uuid
          nullable: true
          type: string
        email:
          description: The email address of the individual
          example: johndoe@example.com
          nullable: true
          type: string
        givenName:
          description: The given name of the individual. This can correspond to first
            and middle names in English.
          example: John Alvin
          nullable: true
          type: string
        familyName:
          description: The family name of the individual
          example: Doe
          nullable: true
          type: string
        fullName:
          description: |-
            Full name of the individual which is composed of the given and family name.
            Unverified full name may be safe to use. For example, Aadhaar card provides full name, but due to certain limitations of verifying Aadhaar, Yoti is unable to verify this property so it is listed as "unverified".
          example: John Alvin Doe
          nullable: true
          type: string
        dateOfBirth:
          description: |-
            The date of birth of the individual.
            Unverified date of birth may be safe to use. For example, Aadhaar card provides date of birth, but due to certain limitations of verifying Aadhaar, Yoti is unable to verify this property so it is listed as "unverified".
          example: 1985-01-31
          format: date
          nullable: true
          type: string
        gender:
          description: "The gender of the individual. Depending on the source or country,\
            \ gender may not be verified. This is a setting that can be configured\
            \ while setting up scopes.\n            \nUnverified gender may be safe\
            \ to use. For example, Aadhaar card provides gender, but due to certain\
            \ limitations of verifying Aadhaar, Yoti is unable to verify this property\
            \ so it is listed as \"unverified\".\nIn some countries\nPossible values:\n\
            - \"MALE\"\n- \"FEMALE\"\n- \"TRANSGENDER\"\n- \"OTHER\""
          example: MALE
          nullable: true
          type: string
        nationality:
          description: The nationality of the individual as a ISO alpha-3 code
          example: GBR
          nullable: true
          type: string
        mobileNumber:
          description: "The mobile phone number of the individual. This number was\
            \ verified with a one time password (OTP) during the user's registration\
            \ with Yoti. In some cases, Yoti may do additional checks against its\
            \ sources to confirm the user's identity."
          example: "+447777123456"
          nullable: true
          type: string
        structuredPostalAddress:
          allOf:
          - $ref: '#/components/schemas/YotiStructuredPostalAddress'
          description: |-
            A structured postal address for the individual that comes from the underlying document or can be manually added. In the case of being manually added, the address is considered "unverified".
            Unverified addresses may be safe to use. For example, Aadhaar card provides an address, but due to certain limitations of verifying Aadhaar, Yoti is unable to verify this property so it is listed as "unverified".
          nullable: true
        postalAddress:
          description: |-
            The whole address for the individual that comes from the underlying document or can be manually added. In the case of being manually added, the address is considered "unverified". The format of this will be different for each country.
            Refer to `StructuredPostalAddress` for a detailed version of the address.
          example: "6th Floor, 107 Leadenhall St, London, EC3A 4AF"
          nullable: true
          type: string
        documentDetails:
          allOf:
          - $ref: '#/components/schemas/YotiDocumentDetails'
          description: The details of the underlying document used to help create
            the Yoti credential
          nullable: true
      type: object
    YotiStructuredPostalAddress:
      properties:
        country:
          description: "The address's country. This field does not have a specified\
            \ format, but is recommended to use for the country field."
          example: UK
          nullable: true
          type: string
        countryIso:
          description: The address's ISO alpha-3 country code
          example: USA
          nullable: true
          type: string
        formattedAddress:
          description: "The whole address in proper order and formatted with line\
            \ breaks.\n            \nExamples:\nUnited Kingdom: \"15a North Street\\\
            nCARSHALTON\\nSM5 2HW\\nUK\"\nIndia: \"S/O: Vipen Kumar Aggarwal\\nHouse\
            \ No.86-A\\nRajguru Nagar\\nLudhiana\\nPunjab\\n141012\\nIndia\"\nUnited\
            \ States of America: \"1512 Ferry Street\\nAnniston AL 36201\\nUSA\""
          example: 15a North Street\nCARSHALTON\nSM5 2HW\nUK
          nullable: true
          type: string
        line1:
          description: The first line of the address
          example: 15a North Street
          nullable: true
          type: string
        line2:
          description: The second line of the address
          example: 15a North Street
          nullable: true
          type: string
        line3:
          description: The third line of the address
          example: 15a North Street
          nullable: true
          type: string
        line4:
          description: The fourth line of the address
          example: 15a North Street
          nullable: true
          type: string
        townCity:
          description: The address's town or city
          example: Los Angeles
          nullable: true
          type: string
        state:
          description: The address's state. This is an optional field that may only
            appear for US addresses.
          example: CA
          nullable: true
          type: string
        postalCode:
          description: The address's postal code
          example: SM5 2HW
          nullable: true
          type: string
        buildingNumber:
          description: The address's building number
          example: 15a
          nullable: true
          type: string
        building:
          description: The address's building name
          example: House No.86-A
          nullable: true
          type: string
        subBuilding:
          description: The address's sub building or specific part of the building
          example: Apt 4B
          nullable: true
          type: string
        locality:
          description: "The address'ss locality can be an area, a village, a region,\
            \ or a known neighborhood"
          example: FRAMINGHAM PIGOT
          nullable: true
          type: string
        deliveryPointReferenceNumber:
          description: The unique delivery point reference number. This is an 8-digit
            code used in the UK for identifying delivery addresses.
          example: "23747208"
          nullable: true
          type: string
        landmark:
          description: The address's landmark. A landmark is a recognizable feature
            or place that helps locate the address. This optional field may appear
            only for Indian addresses.
          example: Behind Bank XYZ
          nullable: true
          type: string
        subdistrict:
          description: The address's subdistrict. This optional field may appear only
            for Indian addresses.
          example: Ludhiana
          nullable: true
          type: string
        district:
          description: The address's district. This optional field may appear only
            for Indian addresses.
          example: Ludhiana
          nullable: true
          type: string
        postOffice:
          description: The address's post office. This optional field may appear only
            for Indian addresses.
          example: Rajguru Nagar
          nullable: true
          type: string
        careOf:
          description: |-
            The address's care-of field. This indicates that mail should be delivered to an individual through another person or entity who is a known resident at the address.
            This optional field may appear only for Indian addresses.
          example: Jane Doe
          nullable: true
          type: string
      type: object
    ZimbabweNidLookup2Input:
      properties:
        idNumber:
          description: The user's National ID number
          nullable: true
          type: string
      type: object
    CreateVerificationProfile_request:
      properties:
        Alias:
          description: An alias of the verification profile shown to developers and
            administrators.
          maxLength: 255
          minLength: 1
          type: string
        BrandName:
          description: The brand name of the verification profile shown to end-users.
          maxLength: 2500
          minLength: 1
          type: string
        PrimaryColor:
          description: |-
            The primary color of the verification profile. Must be a 6-character hex string prefixed with a '#' character.
            Example: #000000
          maxLength: 7
          minLength: 7
          type: string
        Providers:
          description: The list of providers you'd like to select for this profile.
            We will not currently enable any providers.
          items:
            type: string
          type: array
        Logo:
          description: The logo of the verification profile.
          format: binary
          type: string
        IsProductionUsage:
          description: |-
            Whether this profile is for production usage. Only applicable for Live environment profiles.
            If not specified for Live profiles, defaults to false (Demo).
          type: boolean
      required:
      - Alias
      - BrandName
      type: object
  securitySchemes:
    Bearer:
      description: Trinsic Connect API Key
      scheme: bearer
      type: http

